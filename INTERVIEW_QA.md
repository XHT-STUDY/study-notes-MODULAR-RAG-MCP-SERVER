# 面试问答实录整理 — Agent 算法 / LLM 应用开发岗

> 基于本项目（Modular RAG MCP Server）的模拟面试整理稿。
> 岗位方向：【Agent 算法】【LLM 应用开发】
> 整理日期：2026-09-09
>
> 每题保留：核心答案要点、关键数字、收束金句。文末附「主动承认的短板清单」与「方法论速查」。

---

## 目录

- [一、项目总览类](#一项目总览类)
- [二、数据摄取类](#二数据摄取类)
- [三、检索与索引类](#三检索与索引类)
- [四、存储与选型类](#四存储与选型类)
- [五、性能与可观测类](#五性能与可观测类)
- [六、方法论与短板清单](#六方法论与短板清单)

---

## 一、项目总览类

### Q1 项目背景、做了什么、带来什么效果

- **背景**：企业规章制度（HR 政策、合规手册）分散多系统，关键词搜索无法理解语义（"加班费"匹配不到"超时工作补偿"），HR 被重复答疑淹没。
- **做了六块**：混合检索管线（Dense+BM25+RRF+可选重排）、可插拔架构（六大组件 Base*+Factory+YAML）、五阶段摄取流水线（多模态图片描述、两级哈希幂等）、MCP Server（stdio 四工具）、评测闭环（黄金集+Ragas+消融）、可观测与 TDD（全链路 trace、Streamlit 六页面、1200+ 测试）。
- **效果**：Hit Rate@10 ≈ 92%；HR 答疑量 -60%；员工自助覆盖率 40%→85%；增量摄取重跑成本≈0；Provider 紧急切换零代码；重排降级时 Hit Rate 仅降 3~5pp 服务不中断。
- **口径诚实声明**：技术指标来自项目实测/CI；业务数字（覆盖率、答疑量）是基于使用情况的合理估算。

### Q2 效果讲得更直观

- **省人力**：一次答疑从"翻 10 分钟 + 问 HR"变成"口语提问几秒返回带引用的条款"，HR 每月省几十小时。
- **省成本**：两级哈希增量摄取，日常更新只处理变更文件，Embedding/Vision API 账单趋零。
- **保可用**：Rerank 失败自动回退 RRF 融合——用户感受是"质量略降"而非"白屏"。
- **快迭代**：消融实验发现"短文档场景重排收益 3~5pp 但延迟翻倍"→ 重排做成开关而非默认；CI 1200+ 测试让重构敢下手；全链路 trace 让 badcase 十几分钟定位到分词根因。
- **一句话**：省人力、省成本、保可用、快迭代。

### Q3 亮点、难点、创新点，和市面 RAG 项目的区别

- **立场**：骨架（向量库+LLM）确实烂大街，差异在"系统工程 + Agent 基础设施"。
- **七点区别**：两段式检索（并行召回+精排）vs 单 top-k；加权 RRF（fuse_with_weights 按查询类型调权）；三级降级链（Rerank→RRF、Vision LLM→规则、Agent→直通检索）；幂等摄取（两级哈希+确定性 ID+remove-before-rebuild）；评测内置（消融一条命令）；白盒追踪（每层中间结果 JSONL）；Prompt 校验和进 CI。
- **四个难点**：中文 BM25 索引/查询分词一致性（jieba+自定义词典，同一份配置）；多模态不引入多模态模型（`[IMAGE:{id}]` 占位符缝合回 chunk）；Agent 循环控制（路由挡简单查询、白名单、步数上限、成本约束）；MCP stdio 约束（stdout 被 JSON-RPC 独占，日志全走 stderr）。
- **创新点定位（诚实）**：无新算法发明；创新在组合与工程化——"RAG 作为 MCP 工具"的定位、加权 RRF+查询路由的自适应融合、零成本规则评估器海选+Ragas 精评的调优方法论。
- **金句**：同样叫 RAG，我的是每一步可观测、每个组件可替换、每次调优可量化、每个外部依赖可降级的系统——这是生产级 Agent 基础设施和 demo 的本质区别。

### Q4 系统架构、使用流程、技术栈

- **五层分层**：MCP 接入层（protocol_handler+4 工具）→ 客户端（Streamlit/CLI）→ 核心层（settings/types/query_engine/agent/trace）→ 摄取层（pipeline→chunking→transform→embedding→storage）→ 组件层（libs 全可插拔）+ 存储（Chroma+BM25 JSON+SQLite）。
- **设计主线**：`src/libs` 每种能力 = Base* ABC + 若干实现 + XFactory 注册表，`provider:` 字段切换后端，零代码改动。
- **两条数据流**：摄取 6 阶段（完整性→加载→分块→变换→编码→存储）；查询 5 步（预处理→并行召回→RRF→可选重排→生成）。Agent 链路可选（理解→路由→循环（白名单+记忆+反思））。
- **使用流程**：bootstrap 一键初始化 → 配 .env → ingest.py 摄取 → 三种消费（CLI/MCP/仪表盘）→ evaluate.py 回归。
- **技术栈速览**：LLM 五家+Vision 三家；Embedding 四家；Chroma；自实现 BM25+jieba；重排三种可选；回答四种模式；MCP stdio；Ragas+Custom；pytest 三层 1200+、Ruff/mypy/CI/Prompt 校验和。

---

## 二、数据摄取类

### Q5 一次 ingest 的全流程（含设计与理由）

- **入口**：递归扫 PDF，单文件失败不中断批次；每阶段 trace+进度回调。
- **Stage 1 完整性**：SHA-256 查 SQLite 历史，命中即跳过——增量摄取的闸门（重跑 1000 个文件只付 3 个的成本）。
- **Stage 2 加载**：MarkItDown 转文本 + PyMuPDF 抽图 `[IMAGE:{id}]` 占位符；版本预检（哈希不同→存旧版快照备回滚）。
- **Stage 3 分块**：递归切分 1000/200；确定性 chunk ID `{doc_id}_{index:04d}_{hash8}`——幂等 Upsert 前提。
- **Stage 4 变换**：ChunkRefiner（LLM 清洗，降级规则）→ MetadataEnricher（title/summary/tags）→ ImageCaptioner（Vision 描述缝合回 chunk）。每级降级：质量可有梯度，流程不能断。
- **Stage 5 编码**：BatchProcessor 按 10 一批；Dense 走 API（限流/重试粒度），Sparse 本地 jieba（BM25 全局统计必须写入期建）；trace 记录每 chunk top-10 词项（排障利器）。
- **Stage 6 存储**：Chroma 覆盖写（同 ID 幂等）；BM25 doc_id=chunk ID 前缀，remove-before-rebuild 防孤儿 posting，chunk_id 对齐 vector ID；图片登记索引。
- **收尾**：成功→mark_success（记逻辑路径）+版本登记+旧版清理（Chroma 按 `(source_path, doc_hash)` 双条件删，防误删同内容他路径副本；图片/历史仅在旧哈希完全取代时清）；失败→mark_failed+部分数据靠确定性 ID 自愈；OrphanGC 兜底。
- **三个词收束**：幂等、降级、可观测。

### Q6 文档格式与 PDF 多栏处理

- **格式**：只支持 PDF（刻意的场景收窄，企业存量 90%+ 是 PDF；BaseLoader 抽象留扩展位）。
- **多栏（诚实）**：无专门多栏解析；MarkItDown/pdfminer 读序推断对规整双栏尚可，复杂排版会交错。三层缓解：overlap=200、ChunkRefiner LLM 局部修复（无法还原全局栏序）、trace 量化影响（制度类文档基本单栏，影响小）。
- **改进方案**：Loader 后分块前加布局感知层——PyMuPDF `get_text("blocks")` 的 bbox 做栏聚类重排；重度方案 PP-Structure/Docling。改造位置不动管线其他阶段，且必须先给黄金集补多栏样本验证。
- **同类边界**：纯扫描件（无文本层）目前不支持，无整页 OCR。

### Q7 分块怎么做、业务流程被切断怎么办

- **机制**：分隔符优先级递归（`\n\n`→`\n`→句子→子句→空格→字符），块不超 1000 就在段落边界收手——**条款型短段落天然完整，通常根本不会被切断**。
- **真的切断时三层防御**：overlap=200（切断点在两个 chunk 各出现一次）；混合检索召回冗余（两半语义相似通常都被召回，LLM 看到拼合的完整流程）；chunk_index+来源可溯源。
- **根治方案**：结构感知切分（按 Markdown 标题切"节"，零成本）；**父子检索/Small-to-Big**（检索小 chunk 保精度、返回父节保完整——把"被找到的粒度"和"被阅读的粒度"解耦，是终极方案；`chunk_index`+`source_ref` 已预留映射条件）。
- **为什么不用大 chunk 一劳永逸**：向量稀释，检索精度崩。

### Q8 chunk 大小怎么平衡

- **两头失败模式**：太大→向量稀释+上下文预算爆炸+引用粒度粗；太小→Hit Rate 好看是陷阱（片段不含完整答案）+生成端碎片化+索引膨胀扰动 IDF。
- **定法四步**：领域先验定区间（条款 200~800 字→搜索 300~2000）→ 控制变量网格（300/500/1000/1500/2000，overlap 20% 联动）→ 双层指标（Hit Rate/MRR + 流程完整性人工抽查）→ 曲线拐点选 **1000/200**（1000 字符≈650~700 token，与业界 512 token 量级经验互相印证）。
- **三个认知**：chunk_size 必须与 top-k 联合调（共同决定送给 LLM 的信息总量）；是语料相关配置不是代码常量（settings.yaml，可按 collection 分）；父子检索落地后此参数敏感性大幅下降。

### Q9 输入数据噪声大、质量差怎么办

- **五类噪声五种刀**：格式噪声（LLM 清洗）、重复噪声（两级哈希+未来 MinHash）、语义噪声（质量门控拒入）、版本噪声（版本治理，最危险——新旧并存答案自相矛盾）、结构噪声（表格/多栏是已知边界）。
- **已有四道防线**：入口去重；ChunkRefiner 清洗（页脚污染向量就是翻 trace before/after 发现并加规则修复的）；版本治理；检索生成端抗噪（BM25 的 IDF 天然压套话、抽取式答案宁差不错）。
- **认账待补**：摄取质量门控（乱码率/正文占比评分，拦在摄取期——**摄取期拦噪声成本 1，检索期 10，用户答案里 100**）；MinHash 近重复检测；质量分进重排 tie-breaker。
- **两条原则**：治理前移分层兜底；黄金集保留脏样本回归用例——"加强清洗"可能变成"误杀好内容"，必须数据背书。

### Q10 离线解析完整流程（照答版）

- 即摄取全流程的另一种问法：触发调度→六阶段→收尾（详见 Q5），主线"离线付清成本，在线只剩轻链路"。
- 补充要点：--dry-run/--force；单文件独立失败不中断；版本预检+快照；变换三级降级；BatchProcessor 分批；三路存储对齐 ID；成功登记/失败自愈/OrphanGC。

### Q11 大模型增强（Q1 做什么 / Q2 元数据 / Q3 对比了吗）

- **做什么**：ChunkRefiner（清洗重组）、MetadataEnricher（title≤150 字符/summary≤500/3~10 tags，格式强约束+确定性解析，降级规则版启发式）、ImageCaptioner（Vision 描述缝合）。逐 chunk 独立、线程池 5 并发、`use_llm` 全可配。作用：索引质量、可检索性与引用展示、多模态可达。
- **元数据四组**：文档继承（source_path/doc_hash/doc_type/title）；分块生成（chunk_id/chunk_index/source_ref）；富化（title/summary/tags/refined_by/enriched_by/image_captions）；图片登记（image_id/file_path/page）。消费方：metadata filter、Citation、版本清理 key。
- **对比了吗（诚实拆答）**：消融体系建了但只覆盖检索配置，变换级 A/B 未做；已有间接证据（trace before/after 定位页脚污染、refined_by 覆盖率统计、最终配置黄金集基线）。**补法**：双 collection（use_llm 开/关）+同检索配置+同黄金集，对比 Hit Rate/MRR/Ragas faithfulness/token 成本；预判脏 PDF 收益显著、原生数字文档趋零→形成按语料档位开关的策略。

### Q12-13 分块策略全景 + 滑动窗口

- **六种策略**：固定长度（底线，无视语义）；递归字符（性价比默认解，吃 Markdown 段落红利）；结构感知（块与逻辑单元对齐，依赖解析质量需尺寸兜底）；语义切分（理论最优，embedding 成本翻倍，短段落收益 marginal）；父子/Small-to-Big（解耦检索与阅读粒度，公认较优解）；摘要/层级索引（管宏观问题，查询粒度不匹配）。
- **项目决策链**：场景画像（条款型、Markdown 有结构、中粒度查询）→ 排除摘要索引/固定长度 → 选递归（确定性是幂等体系前提）→ 排除语义切分（成本收益不匹配）→ 结构感知列升级、父子列演进 → 参数黄金集网格 → ChunkRefiner 兜底+trace 可归因。
- **滑窗回答**：是"带语义边界吸附的滑窗"——窗口 1000、重叠 200、有效步长 800；纯滑窗 vs 边界吸附的区别；重叠 20% 平衡"上下文延续"与"相邻块重复挤占召回名额"；根治靠父子检索。
- **易混点澄清**：Agent 会话记忆窗口（recent 10 轮）是运行期对话窗口，与摄取期知识切分窗口是两回事。

---

## 三、检索与索引类

### Q14 为什么混合检索

- **互补原理**：Dense 强语义弱精确（低频词/编号/专有名词被平均掉）；BM25 强精确弱改写（字面零重合即失明）。企业查询天然两态（口语化+精确引用），单路必挂一半。
- **例子**："加班费怎么算" vs 原文"超时工作补偿标准"——BM25 零分、dense 一发入魂；"十三薪发放条件"——低频词被稀释 dense 排到第 8，BM25 靠高 IDF 顶到第一。
- **消融数据**：dense 0.72/0.65/0.78；BM25 0.68/0.71/0.73；hybrid RRF **0.80/0.78/0.84**（+20ms）；+CE 0.85/0.82/0.88。混合相对单路 NDCG +6~11 点，是全系统性价比最高的交易。
- **RRF 而非加权求和**：按排名融合（k=60）绕开两路分数量纲不可比。
- **是不是跟风**：业界是先验、badcase 是动机（精确术语类失败查询归因到 embedding 低频词失效）、消融是证据——三样齐了才叫"为什么"。反例：纯口语场景 dense 单路够，纯精确引用小语料 BM25 单路更优。

### Q15 TopK 选多少

- **三层漏斗 20→10→5，各有依据**：召回层 20（Recall 优先——召回漏的精排救不回；黄金集 Recall@K 曲线 20 后走平；两路重合 30~40%，给 RRF 留足候选池；BM25 本地十几 ms 加宽免费）；融合层 10（精度转向关口，top10 后人工相关性断崖）；重排层 5（生成端预算倒推：5×1000 字符≈3300 token 高效区间）。
- **调参史**：早期 10/5 Recall 偏低→放宽召回层→生成端被噪声干扰→收紧重排层到 5。
- **边界**：聚合型查询（"所有涉及 X 的规定"）top-5 不够，正解是查询分解或父子检索，不是无脑加大 K。

### Q16 BM25 / 维度 / 嵌入模型

- **BM25 是倒排索引不是稀疏向量**：SparseEncoder（jieba 分词→清洗→小写→长度过滤→词频/文档长度/唯一词数，与查询侧共享同一分词逻辑）→ BM25Indexer（IDF=log((N-df+0.5)/(df+0.5))，posting 自包含 chunk_id/tf/doc_length，JSON 落盘，k1=1.5、b=0.75，remove-before-rebuild）。
- **为什么倒排不用 SPLADE**：分词可控（中文命门）、得分可逐项拆解归因、零外部依赖。
- **维度**：稀疏侧无维度概念（词表随语料动态，这是倒排与稀疏向量的本质区别）；Dense 1024 维。
- **Chroma 不支持稀疏**——这正是自实现 BM25 独立成第三存储的原因；Qdrant/Pinecone 预留位支持原生稀疏，迁移时 BM25 通道可平滑替换。
- **嵌入模型**：text-embedding-v3（中文第一梯队、OpenAI 兼容接口契合 Provider 抽象、1024 维平衡点、Ollama 离线退路）。**换模型=全量重建索引**（向量空间不兼容）；确定性 chunk ID 让重建只重跑 Embedding 阶段。

### Q17 Index 怎么做的

- **双索引并行构建**，共享同一套确定性 chunk ID（稀疏命中可直接回向量库取原文，融合层按 ID 对齐）。
- **Chroma**：`hnsw:space=cosine`；记录构造 id/vector/metadata（含原文全文——检索命中即得原文，库里是摄取时刻快照）；`_sanitize_metadata` 处理列表字段；同 ID 覆盖写幂等；delete_by_metadata 支持版本清理。
- **BM25**：三步建（语料统计→逐词项 IDF+posting→JSON 落盘）；查询期 BM25 公式累加、未命中词项跳过；更新 remove-before-rebuild 保证 IDF 语义一致无孤儿。
- **当前实况**：default collection 135 chunk、词表 5001、平均文档长度 184。
- **为什么介质不同**：向量要 ANN（值得 HNSW），倒排纯查表（JSON 透明可 diff）——**介质跟着读写模式走**。
- **一致性哲学**：四个存储无分布式事务，靠"同一套确定性身份散落各处"收敛一致，残留 OrphanGC 兜底。

### Q18 召回 Chunk 有噪声，如何保证结果稳定

- **"稳定"三层含义**：质量稳定、服务稳定、行为可预期。
- **四层纵深**：索引挡噪（清洗+版本治理——过期内容是最毒噪声）；融合抗噪（**RRF 双路共识加分机制**：两路都召回的贡献分累加，噪声很少同时骗过两条通道；20→10→5 漏斗逐层挤出）；重排滤噪（Precision 0.80→0.85，失败降级回融合排名保可用）；生成端限损（抽取式只从原文抽+强制 Citation——噪声破坏半径有上界，永不升级为幻觉；LLM 模式 Prompt 约束仅依据上下文+可溯源+低置信明说）。
- **稳定是测出来的**：黄金集含脏场景用例防漂移；trace 让质量抖动可归因。
- **认账两项**：score threshold 阈值过滤未做（需配拒答策略才完整）；质量分 tie-breaker 未落地。

### Q19 检索相关性差的原因与优化

- **五段病因**：数据段（分块不当/脏数据/版本混杂/模型不匹配）；查询段（词汇失配/过短过泛/多意图/多轮指代）；召回段（单路盲区）；排序段（无精排/参数失衡/过滤条件错）；评测段（看不见问题本身就是病因）。
- **诊断先行**：Hit Rate 低→病在召回前；Hit 高 NDCG 低→病在排序。两次真实排障：专有名词检索不到→trace 显示 dense 第 8/BM25 未命中→jieba 切碎"十三薪"→自定义词典；页脚污染→翻 trace before/after→补规则。
- **优化梯次**：第 0 步归因能力（黄金集分层指标+trace）；第一梯队近零成本（混合检索+RRF、查询清洗/改写、漏斗参数）；第二梯队指标驱动（重排 0.84→0.88、分块修正、数据治理门控）；第三梯队重建级（换 embedding 模型、HyDE、查询分解、父子检索）。
- **两条反模式**：不归因直接堆技术（收益递减成本指数）；只管检索不看生成端引用（Citation 是最真实的免费评测信号）。

### Q20 Embedding 模型怎么选、选错什么现象

- **五维选型**：语言领域匹配（一票否决）；黄金集实测（榜单是先验不是证据）；维度成本三角（1024 是平衡点）；部署形态（API+Ollama 双路）；一致性与可迁移（查询文档同模型同版本、长度上限覆盖 chunk、换模型=全量重建）。
- **选错症状学**：语言不匹配→"加了 BM25 效果反而大涨"（dense 没在干活）、正误对相似度挤在窄带；两侧模型不一致→召回近似随机；**索引混装两套空间**→效果时好时坏极难排查（升级服务只重建一半时发生）；长度截断→**长文档靠后章节系统性召回失败**（拿尾部文字直查零命中即实锤）；领域错乱→专业术语相似度反直觉。
- **诊断流水**：黄金集分层→相似度分布分离度检查→badcase 三角验证（dense/BM25 双排名对照）。

---

## 四、存储与选型类

### Q21 存了哪些数据、怎么存

- **六类五介质**：Chroma 向量库（id+1024 维向量+metadata 含原文全文）；BM25 JSON 倒排（只读查表、可 diff 可肉眼检查）；SQLite ingestion_history（file_hash PK/status/——幂等锚点）；SQLite document_versions（(source_path,collection) 与 (file_hash,is_active) 双索引支撑预检与清理判定）；图片索引 SQLite+文件树本体（大对象不进库）；traces.jsonl（只追加流式处理）。
- **data/versions/**：账本记逻辑、快照存实体，回滚按账本找快照以原逻辑路径重摄（chunk ID 不变）。
- **两条主线**：身份体系统一（chunk 级确定性 ID、文件级 SHA-256，跨存储对账不靠事务靠同身份散落）；Local-First（无外部数据库服务，部署=一个环境+一个 data 目录，备份=拷目录；代价：无并发写、无横向扩展——Qdrant/Pinecone 预留位）。
- **金句**：内容进向量库、词项进倒排、状态进 SQLite、大对象进文件树、过程进 JSONL。

### Q22 向量库多大、多少 PDF（实测口径）

- **当前仓库实测**：Chroma 12 MB；BM25 3.1 MB；data/ 总 42 MB；182 条向量（default 135 + 测试 collection）；摄取历史 9 成功 1 失败（5 份真实样例 PDF + 5 个临时测试文件）。
- **口径诚实**：这是开发/演示库；"2000+ 篇"是实际部署语料口径，不在本仓库。两套数字主动掰开，不混淆。
- **容量模型**：~90 KB/chunk（向量 4KB + 原文元数据大头 + HNSW 簿记）；外推 2000 篇≈6 万 chunk≈5.4 GB——嵌入式 Chroma 舒适区；真正天花板是并发写不是容量，百万级之上换 C/S 向量库（Provider 位已留）。
- 42 MB 里向量库只占 12 MB，大头脑是版本快照和图片——印证"大对象进文件树"。

### Q23 什么数据适合/不适合放 RAG

- **主原则**：变更频率 × 查询模式。RAG 为"量大、读多写少、按语义片段查询"而生。
- **适合**：制度/手册（低频变更+语义查询+要出处）、FAQ、非结构化知识沉淀。共同画像：写少读多、语义查询、非结构化、要引用。
- **不适合六类**：高频事务数据（数据库/缓存）；精确等值/聚合（SQL/API）；全局计算（BI）；极小知识集（<50 条 Prompt 直塞）；高密敏感（密钥管理）；严格 1:1 映射（字典/规则引擎）。表格跨行聚合是已知边界。
- **项目三个真实取舍**：Prompt 模板（4 个文件走文件系统+CI 校验）、摄取历史/版本账本（SQLite 直查）、黄金集（考卷不能进教材——查询会召回自己的标准答案）。
- **代价不对称**：该进没进→立刻发现纠错快；不该进了→摄取失血+索引稀释+答案悄悄过期，暴露慢。**入库前三问：变更频率？语义片段还是精确记录？要出处吗？两问以上指向 RAG 才进**。

### Q24 为什么不用 LlamaIndex

- **先纠偏**：不是零框架——langchain-text-splitters、langchain-community（ragas 兼容）、MarkItDown、Chroma、ragas 都是开源件。真问题是"为什么不让框架当骨架"。
- **三个刚需框架给不了**：核心环节控制权（自实现 BM25 的中文分词/加权 RRF/三层漏斗都是非标准件）；白盒可观测（框架事件粒度拿不到"RRF 每个 chunk 的贡献分拆解"，两个真实 badcase 都靠自定义 trace 里非标准中间量破的案）；依赖治理（uv.lock 里 ragas 0.4.3 要求 langchain-community<0.4.2 的活证据——框架越厚，别人的 breaking change 越容易变成你的事故）。
- **何时反过来选它**：PoC 快速验证（两天 demo）、个人小工具——框架舒适区。我们的场景（长期迭代、每层要归因、中文分词要自定义、评测要消融）每条都在框架舒适区外。
- **金句**：框架当零件用，不当骨架用；骨架自研、肌肉开源；标准要遵守（MCP 协议），框架要慎用。

### Q25 PDF 转 Markdown 四子题

- **总框架**：解析归解析（MarkItDown/PyMuPDF 忠实转换）、清洗归清洗（ChunkRefiner 规则打底+LLM 升级+失败降级），分层解耦独立演进。
- **乱码**：SparseEncoder 词项过滤（纯标点/长度<2 丢弃）+规则清洗+LLM 轻度修复。认账：重度乱码不检测不修复，被动降权入库。改进：摄取质量门控（PUA 字符占比/替换符密度评分拒入）。
- **图片**：抽取落盘（data/images/{doc_hash}/）→占位符插入→Vision 描述缝合回 chunk→索引登记（多模态 Citation 可回看原图）。亮点：下游全链路不感知多模态。认账：占位符在页尾非精确文内位置、复杂图表描述有限、装饰图标浪费 Vision 调用。
- **页眉页脚**：规则主力（10+ 横线+Page/Footer/©/Confidential 组合模式；来自真实 badcase 归因而非拍脑袋）+LLM 兜底。认账：纯文字型页眉（无分隔线）规则抓不到。改进：bbox 坐标级过滤（页顶/底固定带+跨页重复性校验，近零误杀）。
- **表格**：MarkItDown 转 Markdown 表格，行内语义基本完整，overlap 保切断，BM25 管表格数字。认账最弱一环：跨行聚合翻车、无边框/合并单元格错乱、跨页大表表头丢失。改进梯次：HTML 表格入 chunk（LLM 理解更好）→跨页表头继承→重聚合需求导出结构化走独立通道。
- **四题共同哲学**：忠实解析→规则打底→LLM 升级→质量门控兜底，全程 before/after trace 可回查。

---

## 五、性能与可观测类

### Q26 时间消耗、延迟测量

- **机制**：两链路全埋点 time.monotonic()，elapsed_ms 进 trace JSONL，仪表盘可视化。
- **查询实测（18 条 trace）**：预处理 340~420ms；BM25 11~94ms；**dense 2100~2700ms（瓶颈在查询向量化的 API 网络往返，不是 Chroma 检索本身）**；融合 <0.1ms；抽取式生成 ~0.3ms；端到端 2.5~3.6s。
- **摄取实测（34 条 trace/单文档）**：加载 63~547ms；变换 2.4~10.9s（占 35~70%，LLM 依赖）；Embedding 3.2~3.6s；存储 485~641ms；总计 6.4~15.9s。
- **嵌入耗时**：摄取侧 ~3.5s/文档（批量 API）；查询侧网络 RTT 是大头。
- **诚实点**：800ms 是同机房/本地 Embedding 前提的设计目标，开发环境"本地服务+远程 Embedding API"达不到——瓶颈清晰在网络往返。优化路径：Ollama 本地（改一行配置）、查询向量缓存、Embedding 就近部署。
- **测量驱动决策的例子**：LLM Rerank 精度略高但延迟数倍（320ms vs 89ms 量级）→ 线上默认关重排、需要时优先 Cross-Encoder——没有数字这个取舍就是玄学。

---

## 六、方法论与短板清单

### 主动承认的短板清单（面试亮点而非减分项）

| 短板 | 现状缓解 | 改进方案 |
|---|---|---|
| 复杂多栏排版 | overlap+LLM 局部修复；场景文档基本单栏 | bbox 栏聚类重排 / PP-Structure，黄金集先补多栏样本 |
| 纯文字型页眉页脚 | LLM 清洗兜底 | 坐标级过滤+跨页重复性校验 |
| 复杂表格（跨行聚合/合并单元格/跨页） | Markdown 表格+overlap+BM25 保底 | HTML 表格入 chunk→表头继承→聚合走结构化通道 |
| 重度乱码/扫描件 | 被动降权（检索不命中） | 摄取质量门控；整页 OCR 链路 |
| 变换级增强 A/B | trace 间接证据+覆盖率统计 | 双 collection 消融（Hit Rate/faithfulness/成本三栏） |
| score threshold 拒答 | top-5 全返回 | 融合/重排分阈值标定+低置信明说 |
| 质量分参与排序 | metadata 里躺着给人看 | 重排 tie-breaker |
| 近重复检测 | 两级哈希只挡完全相同 | MinHash/SimHash 聚类去重 |
| 父子检索 | overlap+召回冗余缓解 | chunk_index+source_ref 已备映射，第一优先级演进项 |

### 方法论速查（贯穿全场的一致模式）

1. **决策三段论**：业界做法是先验、badcase 是动机、消融数据是证据——三样齐了才回答"为什么"。
2. **调参公式**：领域先验定区间 → 黄金集控制变量扫描 → 检索/生成双层指标验收 → 参数进配置承认语料相关性。
3. **稳定性哲学**：不追求零噪声/零故障，追求影响有界（降级链）、来源可归因（trace）、劣化可被逮住（回归测试）。
4. **成本次序**：便宜方案先上（递归+overlap）→ 指标证明不够 → 再上贵的（父子检索）；治理前移（摄取期拦噪声成本 1 < 检索期 10 < 用户答案里 100）。
5. **架构哲学**：骨架自研（六个 Base* 接口）、肌肉开源（分块/存储/评估/解析）；标准要遵守（MCP）、框架要慎用（LlamaIndex）；介质跟着读写模式走；一致性靠统一身份不靠事务。
6. **诚实的口径纪律**：实测数字 vs 估算数字主动掰开；演示库规模 vs 生产语料规模不混淆——说得出"哪里不行、为什么、怎么补"比假装全都行更接近工程真相。

### 关键数字备忘

| 项 | 值 |
|---|---|
| chunk_size / overlap | 1000 字符 / 200（≈650~700 token） |
| TopK 漏斗 | 召回 20/20 → 融合 10 → 重排 5 |
| RRF k | 60 |
| BM25 参数 | k1=1.5, b=0.75 |
| Dense 维度 | 1024（text-embedding-v3） |
| 消融 NDCG@10 | dense 0.78 / BM25 0.73 / hybrid 0.84 / +CE 0.88 / +LLM rerank 0.87 |
| 消融延迟 | 45 / 28 / 65 / 89 / 320 ms |
| 演示库实况 | 5 PDF / 135 chunk / 182 向量 / 12MB Chroma / 词表 5001 |
| 摄取单文档 | 6.4~15.9s（变换 35~70%、嵌入 ~3.5s） |
| 查询端到端（开发环境） | 2.5~3.6s（瓶颈：Embedding API RTT） |
| 测试 | 1200+（Unit/Integration/E2E，非 LLM 测试进 CI） |
| 黄金集 | ~200 条 |
