# 面试问答实录整理 — Agent 算法 / LLM 应用开发岗

> 基于本项目（Modular RAG MCP Server）的模拟面试整理稿。
> 岗位方向：【Agent 算法】【LLM 应用开发】
> 整理日期：2026-09-09（首次）｜ 2026-09-10（追加 Q27~Q38：性能并发、MCP 安全、生产化、评估、意图识别、精排、文档更新、RAG 新方向、Agentic RAG）
>
> 每题保留：核心答案要点、关键数字、收束金句。文末附「主动承认的短板清单」与「方法论速查」。

---

## 目录

- [一、项目总览类](#一项目总览类)
- [二、数据摄取类](#二数据摄取类)
- [三、检索与索引类](#三检索与索引类)
- [四、存储与选型类](#四存储与选型类)
- [五、性能与可观测类](#五性能与可观测类)
- [六、方法论与短板清单](#六方法论与短板清单)
- [七、性能与并发类](#七性能与并发类)
- [八、MCP 实现与安全类](#八mcp-实现与安全类)
- [九、生产化与质量保障类](#九生产化与质量保障类)
- [十、Agent 与前沿方向类](#十agent-与前沿方向类)

---

## 一、项目总览类

### Q1 项目背景、做了什么、带来什么效果

- **背景**：企业规章制度（HR 政策、合规手册）分散多系统，关键词搜索无法理解语义（"加班费"匹配不到"超时工作补偿"），HR 被重复答疑淹没。
- **做了六块**：混合检索管线（Dense+BM25+RRF+可选重排）、可插拔架构（六大组件 Base*+Factory+YAML）、五阶段摄取流水线（多模态图片描述、两级哈希幂等）、MCP Server（stdio 四工具）、评测闭环（黄金集+Ragas+消融）、可观测与 TDD（全链路 trace、Streamlit 六页面、1200+ 测试）。
- **效果**：Hit Rate@10 ≈ 92%；HR 答疑量 -60%；员工自助覆盖率 40%→85%；增量摄取重跑成本≈0；Provider 紧急切换零代码；重排降级时 Hit Rate 仅降 3~5pp 服务不中断。
- **口径诚实声明**：技术指标来自项目实测/CI；业务数字（覆盖率、答疑量）是基于使用情况的合理估算。

### Q2 效果讲得更直观

- **省人力**：一次答疑从"翻 10 分钟 + 问 HR"变成"口语提问几秒返回带引用的条款"，HR 每月省几十小时。
- **省成本**：两级哈希增量摄取，日常更新只处理变更文件，Embedding/Vision API 账单趋零。
- **保可用**：Rerank 失败自动回退 RRF 融合——用户感受是"质量略降"而非"白屏"。
- **快迭代**：消融实验发现"短文档场景重排收益 3~5pp 但延迟翻倍"→ 重排做成开关而非默认；CI 1200+ 测试让重构敢下手；全链路 trace 让 badcase 十几分钟定位到分词根因。
- **一句话**：省人力、省成本、保可用、快迭代。

### Q3 亮点、难点、创新点，和市面 RAG 项目的区别

- **立场**：骨架（向量库+LLM）确实烂大街，差异在"系统工程 + Agent 基础设施"。
- **七点区别**：两段式检索（并行召回+精排）vs 单 top-k；加权 RRF（fuse_with_weights 按查询类型调权）；三级降级链（Rerank→RRF、Vision LLM→规则、Agent→直通检索）；幂等摄取（两级哈希+确定性 ID+remove-before-rebuild）；评测内置（消融一条命令）；白盒追踪（每层中间结果 JSONL）；Prompt 校验和进 CI。
- **四个难点**：中文 BM25 索引/查询分词一致性（jieba+自定义词典，同一份配置）；多模态不引入多模态模型（`[IMAGE:{id}]` 占位符缝合回 chunk）；Agent 循环控制（路由挡简单查询、白名单、步数上限、成本约束）；MCP stdio 约束（stdout 被 JSON-RPC 独占，日志全走 stderr）。
- **创新点定位（诚实）**：无新算法发明；创新在组合与工程化——"RAG 作为 MCP 工具"的定位、加权 RRF+查询路由的自适应融合、零成本规则评估器海选+Ragas 精评的调优方法论。
- **金句**：同样叫 RAG，我的是每一步可观测、每个组件可替换、每次调优可量化、每个外部依赖可降级的系统——这是生产级 Agent 基础设施和 demo 的本质区别。

### Q4 系统架构、使用流程、技术栈

- **五层分层**：MCP 接入层（protocol_handler+4 工具）→ 客户端（Streamlit/CLI）→ 核心层（settings/types/query_engine/agent/trace）→ 摄取层（pipeline→chunking→transform→embedding→storage）→ 组件层（libs 全可插拔）+ 存储（Chroma+BM25 JSON+SQLite）。
- **设计主线**：`src/libs` 每种能力 = Base* ABC + 若干实现 + XFactory 注册表，`provider:` 字段切换后端，零代码改动。
- **两条数据流**：摄取 6 阶段（完整性→加载→分块→变换→编码→存储）；查询 5 步（预处理→并行召回→RRF→可选重排→生成）。Agent 链路可选（理解→路由→循环（白名单+记忆+反思））。
- **使用流程**：bootstrap 一键初始化 → 配 .env → ingest.py 摄取 → 三种消费（CLI/MCP/仪表盘）→ evaluate.py 回归。
- **技术栈速览**：LLM 五家+Vision 三家；Embedding 四家；Chroma；自实现 BM25+jieba；重排三种可选；回答四种模式；MCP stdio；Ragas+Custom；pytest 三层 1200+、Ruff/mypy/CI/Prompt 校验和。

---

## 二、数据摄取类

### Q5 一次 ingest 的全流程（含设计与理由）

- **入口**：递归扫 PDF，单文件失败不中断批次；每阶段 trace+进度回调。
- **Stage 1 完整性**：SHA-256 查 SQLite 历史，命中即跳过——增量摄取的闸门（重跑 1000 个文件只付 3 个的成本）。
- **Stage 2 加载**：MarkItDown 转文本 + PyMuPDF 抽图 `[IMAGE:{id}]` 占位符；版本预检（哈希不同→存旧版快照备回滚）。
- **Stage 3 分块**：递归切分 1000/200；确定性 chunk ID `{doc_id}_{index:04d}_{hash8}`——幂等 Upsert 前提。
- **Stage 4 变换**：ChunkRefiner（LLM 清洗，降级规则）→ MetadataEnricher（title/summary/tags）→ ImageCaptioner（Vision 描述缝合回 chunk）。每级降级：质量可有梯度，流程不能断。
- **Stage 5 编码**：BatchProcessor 按 10 一批；Dense 走 API（限流/重试粒度），Sparse 本地 jieba（BM25 全局统计必须写入期建）；trace 记录每 chunk top-10 词项（排障利器）。
- **Stage 6 存储**：Chroma 覆盖写（同 ID 幂等）；BM25 doc_id=chunk ID 前缀，remove-before-rebuild 防孤儿 posting，chunk_id 对齐 vector ID；图片登记索引。
- **收尾**：成功→mark_success（记逻辑路径）+版本登记+旧版清理（Chroma 按 `(source_path, doc_hash)` 双条件删，防误删同内容他路径副本；图片/历史仅在旧哈希完全取代时清）；失败→mark_failed+部分数据靠确定性 ID 自愈；OrphanGC 兜底。
- **三个词收束**：幂等、降级、可观测。

### Q6 文档格式与 PDF 多栏处理

- **格式**：只支持 PDF（刻意的场景收窄，企业存量 90%+ 是 PDF；BaseLoader 抽象留扩展位）。
- **多栏（诚实）**：无专门多栏解析；MarkItDown/pdfminer 读序推断对规整双栏尚可，复杂排版会交错。三层缓解：overlap=200、ChunkRefiner LLM 局部修复（无法还原全局栏序）、trace 量化影响（制度类文档基本单栏，影响小）。
- **改进方案**：Loader 后分块前加布局感知层——PyMuPDF `get_text("blocks")` 的 bbox 做栏聚类重排；重度方案 PP-Structure/Docling。改造位置不动管线其他阶段，且必须先给黄金集补多栏样本验证。
- **同类边界**：纯扫描件（无文本层）目前不支持，无整页 OCR。

### Q7 分块怎么做、业务流程被切断怎么办

- **机制**：分隔符优先级递归（`\n\n`→`\n`→句子→子句→空格→字符），块不超 1000 就在段落边界收手——**条款型短段落天然完整，通常根本不会被切断**。
- **真的切断时三层防御**：overlap=200（切断点在两个 chunk 各出现一次）；混合检索召回冗余（两半语义相似通常都被召回，LLM 看到拼合的完整流程）；chunk_index+来源可溯源。
- **根治方案**：结构感知切分（按 Markdown 标题切"节"，零成本）；**父子检索/Small-to-Big**（检索小 chunk 保精度、返回父节保完整——把"被找到的粒度"和"被阅读的粒度"解耦，是终极方案；`chunk_index`+`source_ref` 已预留映射条件）。
- **为什么不用大 chunk 一劳永逸**：向量稀释，检索精度崩。

### Q8 chunk 大小怎么平衡

- **两头失败模式**：太大→向量稀释+上下文预算爆炸+引用粒度粗；太小→Hit Rate 好看是陷阱（片段不含完整答案）+生成端碎片化+索引膨胀扰动 IDF。
- **定法四步**：领域先验定区间（条款 200~800 字→搜索 300~2000）→ 控制变量网格（300/500/1000/1500/2000，overlap 20% 联动）→ 双层指标（Hit Rate/MRR + 流程完整性人工抽查）→ 曲线拐点选 **1000/200**（1000 字符≈650~700 token，与业界 512 token 量级经验互相印证）。
- **三个认知**：chunk_size 必须与 top-k 联合调（共同决定送给 LLM 的信息总量）；是语料相关配置不是代码常量（settings.yaml，可按 collection 分）；父子检索落地后此参数敏感性大幅下降。

### Q9 输入数据噪声大、质量差怎么办

- **五类噪声五种刀**：格式噪声（LLM 清洗）、重复噪声（两级哈希+未来 MinHash）、语义噪声（质量门控拒入）、版本噪声（版本治理，最危险——新旧并存答案自相矛盾）、结构噪声（表格/多栏是已知边界）。
- **已有四道防线**：入口去重；ChunkRefiner 清洗（页脚污染向量就是翻 trace before/after 发现并加规则修复的）；版本治理；检索生成端抗噪（BM25 的 IDF 天然压套话、抽取式答案宁差不错）。
- **认账待补**：摄取质量门控（乱码率/正文占比评分，拦在摄取期——**摄取期拦噪声成本 1，检索期 10，用户答案里 100**）；MinHash 近重复检测；质量分进重排 tie-breaker。
- **两条原则**：治理前移分层兜底；黄金集保留脏样本回归用例——"加强清洗"可能变成"误杀好内容"，必须数据背书。

### Q10 离线解析完整流程（照答版）

- 即摄取全流程的另一种问法：触发调度→六阶段→收尾（详见 Q5），主线"离线付清成本，在线只剩轻链路"。
- 补充要点：--dry-run/--force；单文件独立失败不中断；版本预检+快照；变换三级降级；BatchProcessor 分批；三路存储对齐 ID；成功登记/失败自愈/OrphanGC。

### Q11 大模型增强（Q1 做什么 / Q2 元数据 / Q3 对比了吗）

- **做什么**：ChunkRefiner（清洗重组）、MetadataEnricher（title≤150 字符/summary≤500/3~10 tags，格式强约束+确定性解析，降级规则版启发式）、ImageCaptioner（Vision 描述缝合）。逐 chunk 独立、线程池 5 并发、`use_llm` 全可配。作用：索引质量、可检索性与引用展示、多模态可达。
- **元数据四组**：文档继承（source_path/doc_hash/doc_type/title）；分块生成（chunk_id/chunk_index/source_ref）；富化（title/summary/tags/refined_by/enriched_by/image_captions）；图片登记（image_id/file_path/page）。消费方：metadata filter、Citation、版本清理 key。
- **对比了吗（诚实拆答）**：消融体系建了但只覆盖检索配置，变换级 A/B 未做；已有间接证据（trace before/after 定位页脚污染、refined_by 覆盖率统计、最终配置黄金集基线）。**补法**：双 collection（use_llm 开/关）+同检索配置+同黄金集，对比 Hit Rate/MRR/Ragas faithfulness/token 成本；预判脏 PDF 收益显著、原生数字文档趋零→形成按语料档位开关的策略。

### Q12-13 分块策略全景 + 滑动窗口

- **六种策略**：固定长度（底线，无视语义）；递归字符（性价比默认解，吃 Markdown 段落红利）；结构感知（块与逻辑单元对齐，依赖解析质量需尺寸兜底）；语义切分（理论最优，embedding 成本翻倍，短段落收益 marginal）；父子/Small-to-Big（解耦检索与阅读粒度，公认较优解）；摘要/层级索引（管宏观问题，查询粒度不匹配）。
- **项目决策链**：场景画像（条款型、Markdown 有结构、中粒度查询）→ 排除摘要索引/固定长度 → 选递归（确定性是幂等体系前提）→ 排除语义切分（成本收益不匹配）→ 结构感知列升级、父子列演进 → 参数黄金集网格 → ChunkRefiner 兜底+trace 可归因。
- **滑窗回答**：是"带语义边界吸附的滑窗"——窗口 1000、重叠 200、有效步长 800；纯滑窗 vs 边界吸附的区别；重叠 20% 平衡"上下文延续"与"相邻块重复挤占召回名额"；根治靠父子检索。
- **易混点澄清**：Agent 会话记忆窗口（recent 10 轮）是运行期对话窗口，与摄取期知识切分窗口是两回事。

---

## 三、检索与索引类

### Q14 为什么混合检索

- **互补原理**：Dense 强语义弱精确（低频词/编号/专有名词被平均掉）；BM25 强精确弱改写（字面零重合即失明）。企业查询天然两态（口语化+精确引用），单路必挂一半。
- **例子**："加班费怎么算" vs 原文"超时工作补偿标准"——BM25 零分、dense 一发入魂；"十三薪发放条件"——低频词被稀释 dense 排到第 8，BM25 靠高 IDF 顶到第一。
- **消融数据**：dense 0.72/0.65/0.78；BM25 0.68/0.71/0.73；hybrid RRF **0.80/0.78/0.84**（+20ms）；+CE 0.85/0.82/0.88。混合相对单路 NDCG +6~11 点，是全系统性价比最高的交易。
- **RRF 而非加权求和**：按排名融合（k=60）绕开两路分数量纲不可比。
- **是不是跟风**：业界是先验、badcase 是动机（精确术语类失败查询归因到 embedding 低频词失效）、消融是证据——三样齐了才叫"为什么"。反例：纯口语场景 dense 单路够，纯精确引用小语料 BM25 单路更优。

### Q15 TopK 选多少

- **三层漏斗 20→10→5，各有依据**：召回层 20（Recall 优先——召回漏的精排救不回；黄金集 Recall@K 曲线 20 后走平；两路重合 30~40%，给 RRF 留足候选池；BM25 本地十几 ms 加宽免费）；融合层 10（精度转向关口，top10 后人工相关性断崖）；重排层 5（生成端预算倒推：5×1000 字符≈3300 token 高效区间）。
- **调参史**：早期 10/5 Recall 偏低→放宽召回层→生成端被噪声干扰→收紧重排层到 5。
- **边界**：聚合型查询（"所有涉及 X 的规定"）top-5 不够，正解是查询分解或父子检索，不是无脑加大 K。

### Q16 BM25 / 维度 / 嵌入模型

- **BM25 是倒排索引不是稀疏向量**：SparseEncoder（jieba 分词→清洗→小写→长度过滤→词频/文档长度/唯一词数，与查询侧共享同一分词逻辑）→ BM25Indexer（IDF=log((N-df+0.5)/(df+0.5))，posting 自包含 chunk_id/tf/doc_length，JSON 落盘，k1=1.5、b=0.75，remove-before-rebuild）。
- **为什么倒排不用 SPLADE**：分词可控（中文命门）、得分可逐项拆解归因、零外部依赖。
- **维度**：稀疏侧无维度概念（词表随语料动态，这是倒排与稀疏向量的本质区别）；Dense 1024 维。
- **Chroma 不支持稀疏**——这正是自实现 BM25 独立成第三存储的原因；Qdrant/Pinecone 预留位支持原生稀疏，迁移时 BM25 通道可平滑替换。
- **嵌入模型**：text-embedding-v3（中文第一梯队、OpenAI 兼容接口契合 Provider 抽象、1024 维平衡点、Ollama 离线退路）。**换模型=全量重建索引**（向量空间不兼容）；确定性 chunk ID 让重建只重跑 Embedding 阶段。

### Q17 Index 怎么做的

- **双索引并行构建**，共享同一套确定性 chunk ID（稀疏命中可直接回向量库取原文，融合层按 ID 对齐）。
- **Chroma**：`hnsw:space=cosine`；记录构造 id/vector/metadata（含原文全文——检索命中即得原文，库里是摄取时刻快照）；`_sanitize_metadata` 处理列表字段；同 ID 覆盖写幂等；delete_by_metadata 支持版本清理。
- **BM25**：三步建（语料统计→逐词项 IDF+posting→JSON 落盘）；查询期 BM25 公式累加、未命中词项跳过；更新 remove-before-rebuild 保证 IDF 语义一致无孤儿。
- **当前实况**：default collection 135 chunk、词表 5001、平均文档长度 184。
- **为什么介质不同**：向量要 ANN（值得 HNSW），倒排纯查表（JSON 透明可 diff）——**介质跟着读写模式走**。
- **一致性哲学**：四个存储无分布式事务，靠"同一套确定性身份散落各处"收敛一致，残留 OrphanGC 兜底。

### Q18 召回 Chunk 有噪声，如何保证结果稳定

- **"稳定"三层含义**：质量稳定、服务稳定、行为可预期。
- **四层纵深**：索引挡噪（清洗+版本治理——过期内容是最毒噪声）；融合抗噪（**RRF 双路共识加分机制**：两路都召回的贡献分累加，噪声很少同时骗过两条通道；20→10→5 漏斗逐层挤出）；重排滤噪（Precision 0.80→0.85，失败降级回融合排名保可用）；生成端限损（抽取式只从原文抽+强制 Citation——噪声破坏半径有上界，永不升级为幻觉；LLM 模式 Prompt 约束仅依据上下文+可溯源+低置信明说）。
- **稳定是测出来的**：黄金集含脏场景用例防漂移；trace 让质量抖动可归因。
- **认账两项**：score threshold 阈值过滤未做（需配拒答策略才完整）；质量分 tie-breaker 未落地。

### Q19 检索相关性差的原因与优化

- **五段病因**：数据段（分块不当/脏数据/版本混杂/模型不匹配）；查询段（词汇失配/过短过泛/多意图/多轮指代）；召回段（单路盲区）；排序段（无精排/参数失衡/过滤条件错）；评测段（看不见问题本身就是病因）。
- **诊断先行**：Hit Rate 低→病在召回前；Hit 高 NDCG 低→病在排序。两次真实排障：专有名词检索不到→trace 显示 dense 第 8/BM25 未命中→jieba 切碎"十三薪"→自定义词典；页脚污染→翻 trace before/after→补规则。
- **优化梯次**：第 0 步归因能力（黄金集分层指标+trace）；第一梯队近零成本（混合检索+RRF、查询清洗/改写、漏斗参数）；第二梯队指标驱动（重排 0.84→0.88、分块修正、数据治理门控）；第三梯队重建级（换 embedding 模型、HyDE、查询分解、父子检索）。
- **两条反模式**：不归因直接堆技术（收益递减成本指数）；只管检索不看生成端引用（Citation 是最真实的免费评测信号）。

### Q20 Embedding 模型怎么选、选错什么现象

- **五维选型**：语言领域匹配（一票否决）；黄金集实测（榜单是先验不是证据）；维度成本三角（1024 是平衡点）；部署形态（API+Ollama 双路）；一致性与可迁移（查询文档同模型同版本、长度上限覆盖 chunk、换模型=全量重建）。
- **选错症状学**：语言不匹配→"加了 BM25 效果反而大涨"（dense 没在干活）、正误对相似度挤在窄带；两侧模型不一致→召回近似随机；**索引混装两套空间**→效果时好时坏极难排查（升级服务只重建一半时发生）；长度截断→**长文档靠后章节系统性召回失败**（拿尾部文字直查零命中即实锤）；领域错乱→专业术语相似度反直觉。
- **诊断流水**：黄金集分层→相似度分布分离度检查→badcase 三角验证（dense/BM25 双排名对照）。

---

## 四、存储与选型类

### Q21 存了哪些数据、怎么存

- **六类五介质**：Chroma 向量库（id+1024 维向量+metadata 含原文全文）；BM25 JSON 倒排（只读查表、可 diff 可肉眼检查）；SQLite ingestion_history（file_hash PK/status/——幂等锚点）；SQLite document_versions（(source_path,collection) 与 (file_hash,is_active) 双索引支撑预检与清理判定）；图片索引 SQLite+文件树本体（大对象不进库）；traces.jsonl（只追加流式处理）。
- **data/versions/**：账本记逻辑、快照存实体，回滚按账本找快照以原逻辑路径重摄（chunk ID 不变）。
- **两条主线**：身份体系统一（chunk 级确定性 ID、文件级 SHA-256，跨存储对账不靠事务靠同身份散落）；Local-First（无外部数据库服务，部署=一个环境+一个 data 目录，备份=拷目录；代价：无并发写、无横向扩展——Qdrant/Pinecone 预留位）。
- **金句**：内容进向量库、词项进倒排、状态进 SQLite、大对象进文件树、过程进 JSONL。

### Q22 向量库多大、多少 PDF（实测口径）

- **当前仓库实测**：Chroma 12 MB；BM25 3.1 MB；data/ 总 42 MB；182 条向量（default 135 + 测试 collection）；摄取历史 9 成功 1 失败（5 份真实样例 PDF + 5 个临时测试文件）。
- **口径诚实**：这是开发/演示库；"2000+ 篇"是实际部署语料口径，不在本仓库。两套数字主动掰开，不混淆。
- **容量模型**：~90 KB/chunk（向量 4KB + 原文元数据大头 + HNSW 簿记）；外推 2000 篇≈6 万 chunk≈5.4 GB——嵌入式 Chroma 舒适区；真正天花板是并发写不是容量，百万级之上换 C/S 向量库（Provider 位已留）。
- 42 MB 里向量库只占 12 MB，大头脑是版本快照和图片——印证"大对象进文件树"。

### Q23 什么数据适合/不适合放 RAG

- **主原则**：变更频率 × 查询模式。RAG 为"量大、读多写少、按语义片段查询"而生。
- **适合**：制度/手册（低频变更+语义查询+要出处）、FAQ、非结构化知识沉淀。共同画像：写少读多、语义查询、非结构化、要引用。
- **不适合六类**：高频事务数据（数据库/缓存）；精确等值/聚合（SQL/API）；全局计算（BI）；极小知识集（<50 条 Prompt 直塞）；高密敏感（密钥管理）；严格 1:1 映射（字典/规则引擎）。表格跨行聚合是已知边界。
- **项目三个真实取舍**：Prompt 模板（4 个文件走文件系统+CI 校验）、摄取历史/版本账本（SQLite 直查）、黄金集（考卷不能进教材——查询会召回自己的标准答案）。
- **代价不对称**：该进没进→立刻发现纠错快；不该进了→摄取失血+索引稀释+答案悄悄过期，暴露慢。**入库前三问：变更频率？语义片段还是精确记录？要出处吗？两问以上指向 RAG 才进**。

### Q24 为什么不用 LlamaIndex

- **先纠偏**：不是零框架——langchain-text-splitters、langchain-community（ragas 兼容）、MarkItDown、Chroma、ragas 都是开源件。真问题是"为什么不让框架当骨架"。
- **三个刚需框架给不了**：核心环节控制权（自实现 BM25 的中文分词/加权 RRF/三层漏斗都是非标准件）；白盒可观测（框架事件粒度拿不到"RRF 每个 chunk 的贡献分拆解"，两个真实 badcase 都靠自定义 trace 里非标准中间量破的案）；依赖治理（uv.lock 里 ragas 0.4.3 要求 langchain-community<0.4.2 的活证据——框架越厚，别人的 breaking change 越容易变成你的事故）。
- **何时反过来选它**：PoC 快速验证（两天 demo）、个人小工具——框架舒适区。我们的场景（长期迭代、每层要归因、中文分词要自定义、评测要消融）每条都在框架舒适区外。
- **金句**：框架当零件用，不当骨架用；骨架自研、肌肉开源；标准要遵守（MCP 协议），框架要慎用。

### Q25 PDF 转 Markdown 四子题

- **总框架**：解析归解析（MarkItDown/PyMuPDF 忠实转换）、清洗归清洗（ChunkRefiner 规则打底+LLM 升级+失败降级），分层解耦独立演进。
- **乱码**：SparseEncoder 词项过滤（纯标点/长度<2 丢弃）+规则清洗+LLM 轻度修复。认账：重度乱码不检测不修复，被动降权入库。改进：摄取质量门控（PUA 字符占比/替换符密度评分拒入）。
- **图片**：抽取落盘（data/images/{doc_hash}/）→占位符插入→Vision 描述缝合回 chunk→索引登记（多模态 Citation 可回看原图）。亮点：下游全链路不感知多模态。认账：占位符在页尾非精确文内位置、复杂图表描述有限、装饰图标浪费 Vision 调用。
- **页眉页脚**：规则主力（10+ 横线+Page/Footer/©/Confidential 组合模式；来自真实 badcase 归因而非拍脑袋）+LLM 兜底。认账：纯文字型页眉（无分隔线）规则抓不到。改进：bbox 坐标级过滤（页顶/底固定带+跨页重复性校验，近零误杀）。
- **表格**：MarkItDown 转 Markdown 表格，行内语义基本完整，overlap 保切断，BM25 管表格数字。认账最弱一环：跨行聚合翻车、无边框/合并单元格错乱、跨页大表表头丢失。改进梯次：HTML 表格入 chunk（LLM 理解更好）→跨页表头继承→重聚合需求导出结构化走独立通道。
- **四题共同哲学**：忠实解析→规则打底→LLM 升级→质量门控兜底，全程 before/after trace 可回查。

---

## 五、性能与可观测类

### Q26 时间消耗、延迟测量

- **机制**：两链路全埋点 time.monotonic()，elapsed_ms 进 trace JSONL，仪表盘可视化。
- **查询实测（18 条 trace）**：预处理 340~420ms；BM25 11~94ms；**dense 2100~2700ms（瓶颈在查询向量化的 API 网络往返，不是 Chroma 检索本身）**；融合 <0.1ms；抽取式生成 ~0.3ms；端到端 2.5~3.6s。
- **摄取实测（34 条 trace/单文档）**：加载 63~547ms；变换 2.4~10.9s（占 35~70%，LLM 依赖）；Embedding 3.2~3.6s；存储 485~641ms；总计 6.4~15.9s。
- **嵌入耗时**：摄取侧 ~3.5s/文档（批量 API）；查询侧网络 RTT 是大头。
- **诚实点**：800ms 是同机房/本地 Embedding 前提的设计目标，开发环境"本地服务+远程 Embedding API"达不到——瓶颈清晰在网络往返。优化路径：Ollama 本地（改一行配置）、查询向量缓存、Embedding 就近部署。
- **测量驱动决策的例子**：LLM Rerank 精度略高但延迟数倍（320ms vs 89ms 量级）→ 线上默认关重排、需要时优先 Cross-Encoder——没有数字这个取舍就是玄学。

---

## 六、方法论与短板清单

### 主动承认的短板清单（面试亮点而非减分项）

| 短板 | 现状缓解 | 改进方案 |
|---|---|---|
| 复杂多栏排版 | overlap+LLM 局部修复；场景文档基本单栏 | bbox 栏聚类重排 / PP-Structure，黄金集先补多栏样本 |
| 纯文字型页眉页脚 | LLM 清洗兜底 | 坐标级过滤+跨页重复性校验 |
| 复杂表格（跨行聚合/合并单元格/跨页） | Markdown 表格+overlap+BM25 保底 | HTML 表格入 chunk→表头继承→聚合走结构化通道 |
| 重度乱码/扫描件 | 被动降权（检索不命中） | 摄取质量门控；整页 OCR 链路 |
| 变换级增强 A/B | trace 间接证据+覆盖率统计 | 双 collection 消融（Hit Rate/faithfulness/成本三栏） |
| score threshold 拒答 | top-5 全返回 | 融合/重排分阈值标定+低置信明说 |
| 质量分参与排序 | metadata 里躺着给人看 | 重排 tie-breaker |
| 近重复检测 | 两级哈希只挡完全相同 | MinHash/SimHash 聚类去重 |
| 父子检索 | overlap+召回冗余缓解 | chunk_index+source_ref 已备映射，第一优先级演进项 |

### 方法论速查（贯穿全场的一致模式）

1. **决策三段论**：业界做法是先验、badcase 是动机、消融数据是证据——三样齐了才回答"为什么"。
2. **调参公式**：领域先验定区间 → 黄金集控制变量扫描 → 检索/生成双层指标验收 → 参数进配置承认语料相关性。
3. **稳定性哲学**：不追求零噪声/零故障，追求影响有界（降级链）、来源可归因（trace）、劣化可被逮住（回归测试）。
4. **成本次序**：便宜方案先上（递归+overlap）→ 指标证明不够 → 再上贵的（父子检索）；治理前移（摄取期拦噪声成本 1 < 检索期 10 < 用户答案里 100）。
5. **架构哲学**：骨架自研（六个 Base* 接口）、肌肉开源（分块/存储/评估/解析）；标准要遵守（MCP）、框架要慎用（LlamaIndex）；介质跟着读写模式走；一致性靠统一身份不靠事务。
6. **诚实的口径纪律**：实测数字 vs 估算数字主动掰开；演示库规模 vs 生产语料规模不混淆——说得出"哪里不行、为什么、怎么补"比假装全都行更接近工程真相。

### 关键数字备忘

| 项 | 值 |
|---|---|
| chunk_size / overlap | 1000 字符 / 200（≈650~700 token） |
| TopK 漏斗 | 召回 20/20 → 融合 10 → 重排 5 |
| RRF k | 60 |
| BM25 参数 | k1=1.5, b=0.75 |
| Dense 维度 | 1024（text-embedding-v3） |
| 消融 NDCG@10 | dense 0.78 / BM25 0.73 / hybrid 0.84 / +CE 0.88 / +LLM rerank 0.87 |
| 消融延迟 | 45 / 28 / 65 / 89 / 320 ms |
| 演示库实况 | 5 PDF / 135 chunk / 182 向量 / 12MB Chroma / 词表 5001 |
| 摄取单文档 | 6.4~15.9s（变换 35~70%、嵌入 ~3.5s） |
| 查询端到端（开发环境） | 2.5~3.6s（瓶颈：Embedding API RTT） |
| 测试 | 1200+（Unit/Integration/E2E，非 LLM 测试进 CI） |
| 黄金集 | ~200 条 |

---

## 七、性能与并发类

### Q27 考虑性能优化了吗？项目中哪些地方可以优化？

考虑了，而且优化哲学是**"测量先行、数据驱动、按 ROI 排序"**——不是凭感觉优化，每一步都有 trace 里的数字背书。分三块：已经做了的优化（有数据支撑的决策）、识别出但还没做的优化清单（按投入产出排序）、以及刻意"不优化"的部分。

**一、已经落地的优化（每条都有测量依据）**

在线查询侧：

1. **双路召回并行执行**：dense 和 BM25 用并行调度（`parallel_retrieval: true`），耗时取 max 而非求和——BM25 本地 11~94ms，基本被 dense 的 API 往返完全掩盖，等于免费；
2. **重排默认关闭 + 分级选择**：测量直接驱动的决策——消融数据显示 LLM Rerank 精度只比 Cross-Encoder 高一点点（NDCG 0.87 vs 0.88 量级）但延迟数倍（320ms vs 89ms 量级），所以线上默认路径不含重排，需要精排的场景优先 Cross-Encoder；
3. **抽取式答案零成本**：无 LLM 依赖时生成 ~0.3ms；
4. **懒加载**：LLM、Embedding、索引都懒初始化，不用的能力不付初始化成本。

离线摄取侧：

1. **增量摄取**：文件级 SHA-256 跳过未变更文件——重跑 1000 个文件只有 3 个真正付成本，这是最大的一项"优化"；
2. **变换阶段线程池并发**：LLM 清洗/富化按 chunk 粒度 5 workers 并行，把 LLM 调用的等待时间重叠掉；
3. **Embedding 批量分批**：batch_size=10 控制限流，失败重试一批而非整个文档。

**二、识别出的优化机会（按 ROI 排序）**

第 1 优先级：**查询向量化的 API 往返——占了端到端的 70%+**。实测 dense 检索 2.1~2.7s，其中大头是查询 Embedding 的网络 RTT，不是 Chroma 检索本身。四个手段按性价比排：查询向量缓存（LRU，查询重复率高的内部场景收益直接）；Embedding 服务就近/本地化（切 Ollama 本地模型是一行配置的事，代价是效果略降——这正是 provider 抽象预留的）；HTTP 连接复用/Keep-Alive（确认 client 没有每次查询重建连接）；预期收益：端到端从 2.5~3.6s 压到 **500ms 以内**，这是全项目最高 ROI 的一刀。

第 2 优先级：**查询预处理的 340~420ms**。每条查询固定付 300ms+ 在分词和关键词提取上。手段：进程启动时预热 jieba 词典（首次加载是大头）、QueryProcessor 实例复用、关键词结果缓存。单看不大，但它是仅次于 embedding 的第二块固定开销。

第 3 优先级：**摄取吞吐（批量场景才值得做）**。变换阶段提高并发（5 workers → 依 provider 限流上调到 10~20）；LLM 清洗/富化合并成一次调用（一个 Prompt 同时出清洗文本+title+tags+summary，省一半往返）；Embedding 批次加大（batch_size 10 → API 允许的 25~50）；文件级并行摄取（多文件流水线重叠——但要注意 SQLite 和 Chroma 嵌入式模式的写锁竞争，这是受架构约束的一步）；Vision 调用按尺寸过滤（装饰性小图标直接跳过 ImageCaptioner）。

第 4 优先级：**存储与索引层（规模上去才动）**。BM25 索引常驻内存（当前每次查询 load JSON 的开销在语料大后会显现）；Chroma 的 HNSW 参数调优（ef_search 精度/延迟权衡）；百万 chunk 之上换 C/S 向量库（Qdrant 预留位）——容量不是动因，**并发写**才是。

第 5 优先级（容易被忽略）：**可观测性自身的开销**。trace 里序列化了 chunk 全文和 before/after 快照，JSONL 写入在高压下会成为负担。手段：大字段截断采样、异步落盘。这是"观测系统的系统也要被观测"的自觉。

**三、刻意"不优化"的部分（同样是工程判断）**

- 召回层加宽到 20 没有收紧：因为实测 BM25 十几毫秒、Chroma 检索不是瓶颈——在瓶颈不在此处时收紧它只损失 Recall；
- 没有上查询结果级缓存：内部场景查询分布长尾，命中率撑不起缓存一致性成本（数据更新后的失效处理比缓存本身复杂）——先测命中率再决定；
- 没有过早做异步化/消息队列：当前是单机内部工具，同步链路的简单性比吞吐更有价值——这是"为当前规模写当前代码"的克制。

一句话总结：**性能优化我们已经过了"拍脑袋加缓存"的阶段——trace 全埋点让瓶颈明码标价（查询侧 Embedding RTT 占 70%，摄取侧 LLM 变换占 35~70%），已做的优化（并行召回、重排分级、增量摄取、批量分批）每条都有消融或延迟数据背书；未做的机会按 ROI 排好了队，第一刀永远是查询向量缓存和 Embedding 就近化，预期把端到端从秒级压到半秒内——而所有优化的验收标准不变：延迟降了，黄金集指标不能掉。**

### Q28 对系统吞吐量做过测试吗？QPS / TPS 是多少？

诚实回答：**没有做过正式的吞吐量压测，给不出实测的 QPS/TPS 数字**——这一题不想编数。但可以给出三样东西：为什么没做（设计定位决定的）、基于实测延迟能推导出的吞吐画像（量级判断）、以及如果需要支撑 N 并发会怎么做（测试方案 + 架构改造清单）。

**一、为什么没做压测：吞吐从来不是这个系统的设计约束**

1. **接入形态决定了天然单用户**：MCP 走 stdio——一个客户端一个进程，协议上就没有"多客户端并发打一个服务"的场景；Streamlit 仪表盘和 CLI 也是单操作者工具。没有并发入口，QPS 就不是一个真实存在的需求指标；
2. **Local-First 的介质选择**：嵌入式 Chroma + SQLite 是按"零运维单机"选的，它们的能力边界在并发写，不在吞吐；
3. **优化目标排序**：这个系统的 SLA 是"单查询延迟 + 答案质量 + 可归因性"，所有测量资源都投在这三项上（trace 全埋点、黄金集、消融）。

**二、吞吐画像可以从实测延迟推导**

查询侧（在线）：实测单查询端到端 2.5~3.6s（瓶颈在 Embedding API 网络往返），由此：**串行吞吐 ≈ 0.3 QPS/进程**——这是当前形态的真实量级。并发上探的空间：延迟构成里约 70% 是网络等待（embedding RTT）、CPU 部分（jieba 分词、RRF 融合 <0.1ms）很轻，理论上线程池并发 10~20 路查询，单进程可到 **3~6 QPS**——但天花板很快会撞到两个地方：**Embedding API 的限流**（外部约束）和 **Python GIL**（jieba 分词段的 CPU 争抢）。再往上要靠多进程 + 前置缓存。参考系：内部知识库工具的真实负载是个位数并发，**0.3 QPS 串行都已接近够用**。

摄取侧（离线，TPS 的对应物）：实测单文档 6.4~15.9s，即**串行 ~0.06~0.15 文档/秒**（约每小时 200~500 篇）；变换阶段线程池 5 并发已把 LLM 等待重叠掉一部分。对"2000 篇存量 + 日常增量"的负载画像，首次全量一天内跑完、增量摄取分钟级——容量是匹配的。

**三、如果要支撑真实并发，会这么做**

先测，测完再改——测试方案本身要专业：

1. **压测设计**：用 locust/k6 或自写 async 驱动，从 1 并发阶梯爬坡到 50；每档记录 **P50/P95/P99 延迟、QPS、错误率**；找到延迟超 SLA 或错误率抬头的**饱和点**，而不是只报峰值 QPS（峰值毫无意义，P99 才是用户体验）；
2. **预期的瓶颈浮现顺序**（赌这个顺序）：Embedding API 限流 → GIL（分词段）→ Chroma 并发读 → SQLite 摄取写锁。压测就是验证这个排序；
3. **配套的黄金集抽检**：高并发下检索质量不能漂（缓存命中旧向量之类的问题），压测期间跑抽样评测。

架构改造清单（按 ROI 排）：查询向量 LRU 缓存（重复查询直接砍掉 RTT）→ Embedding 本地化/就近部署（消灭 70% 的等待）→ 包一层 **FastAPI 异步 HTTP 服务**（替换 stdio 成为多客户端入口，async 天然适合这种 I/O 密集负载）→ 多 worker 进程横向扩 → 百万级语料或真高并发时迁 Qdrant（Provider 位已留，上层代码不动）。

一句话：**没做过压测、没有实测 QPS 数字——这是单用户 stdio + Local-First 定位下的合理取舍；但基于实测延迟能给出量级画像（串行 ~0.3 QPS，线程化后 3~6 QPS，瓶颈在 Embedding API 和 GIL），也有一套从压测设计到架构扩容的完整路径**。如果岗位场景是真并发生产服务，会把"FastAPI 异步化 + 压测饱和点报告"列为第一周的作业。

### Q29 能支持用户并发吗？能并发上传文档吗？能并发提问吗？如何实现并发？（设计方案）

先给结论：**并发提问——当前架构稍加包装就能支持；并发上传——当前架构不安全，必须改造成"单写者"模型；用户并发——取决于前两者，按分级演进路线走**。

**一、三问直答**

| 问题 | 现状 | 原因 |
|---|---|---|
| 能并发提问吗 | **基本能**（单进程内异步并发已可行，跨进程只读并发可行） | 查询链路是 **I/O 密集**：~70% 耗时在 Embedding API 网络等待，CPU 段极轻（RRF 融合 <0.1ms、分词毫秒级）——天然适合并发 |
| 能并发上传文档吗 | **不能，有数据竞态** | 三个写入点都没有并发控制：BM25 索引是**整文件重写**（last-writer-wins，互相覆盖丢数据）；SQLite 有库级写锁（并发写报 busy）；嵌入式 Chroma 多进程写有锁竞争和一致性风险 |
| 能支持用户并发吗 | 有限 | 入口就是瓶颈：MCP stdio 一个客户端一个进程，没有多用户入口 |

**二、现状分析：读写路径的并发禀赋完全不同**

查询路径（读）——禀赋好：延迟构成里 dense 检索 2.1~2.7s 大头是 Embedding API 的 RTT（I/O 等待），本地计算（BM25 查询十几 ms、融合 0.1ms、抽取式生成 0.3ms）极轻；I/O 等待意味着 asyncio/线程池能让几十路查询的等待重叠而不互相拖累；读操作天然可复制（BM25 JSON 多进程各加载一份、嵌入式 Chroma 支持多进程并发读）；唯一 CPU 争抢点是 GIL 下的 jieba 分词，但只占单查询几十 ms。

摄取路径（写）——三处硬伤：**BM25 JSON 是最脆的**（remove-before-rebuild 是"读旧文件→内存重建→整文件写回"，两个摄取并发执行时后写者覆盖前写者，前一个文档的 posting 直接丢失——静默数据丢失，比报错更糟）；SQLite 单写者数据库（并发写触发锁等待或 busy 错误，`file_hash PRIMARY KEY` 并发插入撞唯一约束）；嵌入式 Chroma 官方定位单进程写；衍生问题：trace JSONL 多进程追加可能交错出坏行、版本账本的 is_active 翻转和旧版清理并发执行互相踩。

**三、设计方案（分级演进）**

L1：查询并发（改造成本最低，先做）——入口异步化：核心引擎外包一层 **FastAPI 异步 HTTP 服务**（保留 MCP stdio 给单客户端场景；MCP 本身也有 streamable HTTP 传输可切换）。设计要点：查询处理器用 async，Embedding 调用用异步客户端让网络等待重叠；Chroma/BM25 同步本地调用丢进线程池（`run_in_executor`）不阻塞事件循环；**查询向量 LRU 缓存**（带 TTL + 索引版本号失效）；BM25 索引进程内常驻单例；**无状态水平扩展**（多 API worker 进程各持只读索引副本，前置网关轮询——查询服务无共享可变状态，加进程即扩容）。预期：单进程 20~50 并发 P95 达标（瓶颈在 Embedding API 限流，需客户端侧限流+排队），多进程线性扩。

L2：摄取并发（核心：单写者 + 队列 + 原子发布）——**原则：并发的是"处理"，串行的是"落库"**：

```text
上传 API ──→ 暂存区(对象存储/临时目录) + 任务入队
                    │
                    ▼
        Ingestion Worker（单写者，可多阶段流水并行）
        解析→分块→变换(线程池)→编码(批量)→串行落库
                    │
                    ▼
        原子发布：BM25 写临时文件 + os.rename 原子替换
                 Chroma/SQLite 由唯一写者顺序写入
                    │
                    ▼
        发布版本指针 → 读端下次查询加载新索引
```

关键设计点：①**任务队列**——轻量方案直接用 SQLite 任务表（状态机 pending→processing→done/failed）+ worker 轮询；规模大了换 Redis/RQ，不引入 Kafka 这种重量；②**幂等去重天然友好**——两级哈希 + 确定性 chunk ID 的幂等设计在这里直接兑现价值：**同一文件被并发上传两次，队列里两条任务先后执行，第二次 Stage 1 命中哈希直接跳过**，天然收敛，不需要分布式锁——这是现有架构里最赚的一笔"并发红利"；③**BM25 原子写**——写 tmp 文件 + `os.replace` 原子替换（同文件系统 rename 原子性），读端要么读到旧索引要么读到新索引，永不读到半个；④**读不到半成品（read-copy-update）**——新版本索引发布前查询服务继续用旧版本，发布即切指针，消除"摄取进行中查询质量抖动"；⑤**SQLite 开 WAL 模式 + busy_timeout**——读写不互斥，写串行排队；⑥**Chroma 单写者进程**——所有 upsert 经 worker 串行执行，或直接上 Chroma HttpClient 模式（独立 server 进程，客户端并发写由 server 串行化）。

L3：真规模化（用户量大/语料百万级才动）——Qdrant 替换嵌入式 Chroma（Provider 位已留，改配置）、Postgres 替换 SQLite（摄取历史/账本/队列三合一）、Redis 做缓存和队列、摄取 worker 多实例分片（按 collection 哈希分片，保证同一 collection 仍然单写者）。每一项都是配置和部署变更，核心代码因抽象接口不动。

**四、一致性与故障配套**

**重试语义**：队列 at-least-once + 摄取幂等 = 事实上的 exactly-once 效果——worker 崩了重跑任务，确定性 ID 保证不产生重复数据；**孤儿兜底**：并发更新下的残留由 OrphanGC 周期回收；**trace 分文件**：每 worker 进程独立 trace 文件（带进程号），消灭多进程追加交错；**背压**：上传 API 限制暂存区配额 + 队列深度告警，防止下游 LLM/Embedding 限流被打爆。

**五、验收标准**

并发改造最容易"看起来能跑"，验收必须量化：**locust 阶梯压测**（1→50 并发查询，P50/P95/P99、错误率、饱和点）+ **并发摄取正确性测试**（10 个进程同时上传 10 份文档，校验索引 chunk 数 = 理论值，无丢失无重复——专测 BM25 竞态那个坑）+ 高压下黄金集抽样（质量不漂移）。

一句话收束：**读路径天生适合并发（I/O 密集 + 无共享状态，L1 异步化+缓存即可支撑几十并发）；写路径的病根是"多写入点无并发控制"，药方是"处理并行化 + 落库单写者 + 原子发布 + 幂等收敛"四级组合；而现有架构的确定性 ID 和两级哈希，恰好是并发方案里最值钱的地基**。

---

## 八、MCP 实现与安全类

### Q30 MCP Server 是如何实现的？如何使用？有什么效果？

**一、实现：三层结构**

```text
SDK 传输层  server.py        官方 MCP Python SDK + stdio
协议层      protocol_handler.py   ProtocolHandler（工具注册/Schema/执行/错误码）
工具层      tools/*.py       4 个工具，核心引擎之上的薄适配器
```

①**SDK 传输层（`server.py`）**：基于官方 MCP Python SDK 的低层 `Server`，stdio 传输——**进程即服务，标准输入输出就是协议信道**。这一层有两个用血泪换来的工程细节：**stdout 协议独占**——启动时把所有 logger 的 handler 替换成 stderr handler，MCP stdio 模式下 stdout 只能走 JSON-RPC 消息，任何一行 print/日志混进去，客户端解析直接崩，这是 stdio 传输最阴的坑；**重依赖预加载防死锁**——MCP SDK 用 anyio + 后台线程做 I/O，工具处理器里 `asyncio.to_thread` 执行时如果才首次 `import chromadb`（连带 onnxruntime/numpy 的 C 扩展），worker 线程会和 stdin 读取线程争 Python 的全局 import 锁，**偶发死锁**。解法是在主线程、anyio 起 I/O 线程之前，把重模块全部预导入——后续线程只查 `sys.modules` 缓存。

②**协议层（`protocol_handler.py`）**：`ProtocolHandler` 封装工具生命周期——`register_tool` 注册（`ToolDefinition` = 名称 + 描述 + JSON Schema 输入定义）、`get_tool_schemas()` 响应 `tools/list`（返回 SDK 的 `types.Tool`）、`execute_tool()` 响应 `tools/call`（返回 `CallToolResult`）、`JSONRPCErrorCodes` 统一错误语义。工厂函数 `create_mcp_server` 把低层 Server 和 handler 缝起来，注册两个 async 处理器。**协议层不认识任何业务**——它只管"工具长什么样、怎么调度"。

③**工具层（`tools/`，四个自包含模块）**：每个工具一个文件，三件套：`TOOL_NAME`、`TOOL_DESCRIPTION`（写给 Agent 看的自然语言说明，Agent 靠它决定何时调用）、`TOOL_INPUT_SCHEMA`（JSON Schema，含类型/默认值/约束/required）。以 `query_knowledge_hub` 为例：schema 定义 `query`（必填）、`top_k`（1~20，默认 5）、`collection`（可选范围限定）；handler 是 async 的，内部调核心引擎 HybridSearch + 可选重排，结果经 `ResponseBuilder` 组装成带**结构化 Citation** 的响应。四个工具：`query_knowledge_hub`（混合检索问答）、`list_collections`（列集合带统计）、`get_document_summary`（按 ID 取摘要元数据）、`agent_query`（Agentic RAG，**Agent 未启用时自动退化为直通混合检索**——降级哲学延伸到了接口层）。

**分层的关键约束**：工具是核心引擎之上的**薄适配器**，检索逻辑、Agent 循环、trace 全部在核心层——MCP 只是众多入口（CLI/仪表盘/协议）之一，任何工具行为都能在 CLI 复现和测试。

**二、使用：三步接入**

1. **起服务**：`uv run python main.py`（要求 `config/settings.yaml` 有效）。注意它不是交互式命令——stdio 被协议占用，别在终端里手敲；
2. **客户端配置**：在 MCP 客户端（Copilot / Claude Desktop / Cursor）里注册一条：

```json
{
  "mcpServers": {
    "modular-rag": {
      "command": "uv",
      "args": ["run", "python", "main.py"],
      "cwd": "F:/path/to/modular-rag-mcp-server"
    }
  }
}
```

3. **对话即用**：客户端握手时 `tools/list` 自动发现四个工具及其 schema——之后在对话里问"公司差旅报销标准是什么"，**Agent 自己决定**调用 `query_knowledge_hub(query="差旅报销标准")`，拿到带引用的结果组织答案。要开 Agent 循环就把 `agent.enabled: true`。

整个接入对使用者零代码、零 SDK 知识——这是 MCP 相对自建 REST API 的本质差异：**不需要写客户端集成代码，工具发现、参数校验、调用协议全是标准化的**。

**三、效果：四个维度**

1. **生态即插即用**：一次实现，Copilot、Claude、Cursor 等所有 MCP 客户端直接可调——知识库从"一个要打开的网页"变成"Agent 工具箱里的一等公民"。员工在写代码、写文档的上下文里直接问制度问题，不用切换工具——这是"员工自助查询覆盖率 40%→85%"里很重要的一个体验杠杆；
2. **私有数据不出本机**：stdio 零网络端口、零鉴权配置，知识库和 Agent 客户端在同一台机器进程间通信——对内部合规文档，这个安全模型比"起个公网 API + 管密钥"简单一个量级；
3. **答案可信**：工具返回结构化 Citation（来源路径、chunk 定位、多模态引用），Agent 的回答能落到具体条款——Agent 生成的内容有出处背书，这直接抑制幻觉；
4. **工程上的效果**：协议层 15% 的代码量换来了 100% 的生态兼容性——如果当初为每个客户端写集成（VS Code 插件、Claude 插件、网页端），维护成本是 N 份；现在是协议 1 份。加上前述两个坑的修复（stdout 污染、import 死锁），服务在真实客户端下长时间稳定运行。

**一句话收束**：实现上是"官方 SDK 传输 + 自研协议调度层 + 四个薄工具适配器"的三层结构，核心复杂性不在协议本身，而在 stdio 的工程约束（stdout 独占、import 锁死锁）和"工具层不碰业务"的分层纪律；使用上是"起进程、配客户端、对话即用"三步；效果上是**用一份协议实现换整个 Agent 生态的即插即用，用本机进程通信换私有数据安全，用结构化引用换 Agent 答案的可信度**。

### Q31 MCP 使用 stdio/stdout 作为传输层，是否安全？会不会有敏感字段泄露的风险？

先给结论：**stdio 本身不是安全弱点，反而是单机场景下的安全增益——真正的泄露风险不在传输层，而在"敏感数据以明文形态存在的三个位置"和"工具载荷天生携带文档内容"**。

**一、先立正确的安全模型：stdio 的信任边界 = 本地会话边界**

stdio 是**进程间管道**，管道两端是 MCP 客户端和服务进程，OS 强制隔离——**其他用户的进程读不到这个管道**；**零网络暴露面**：没有监听端口、没有 TLS 证书、没有鉴权接口、没有公网可达的攻击面。对比 HTTP 方案要操心的事——端口扫描、中间人、Token 泄露、CORS——stdio 一件都没有；信任假设清晰：**能发起连接的主体 = 能在你机器上拉起进程的主体 = 你自己的 OS 会话**。所以单用户本地场景，**stdio 比自建 HTTP API 更安全**（攻击面是减法不是加法）。但"传输层安全"不等于"系统安全"——泄露风险在别处。

**二、真实的敏感字段泄露风险：三层枚举**

①传输层风险：**stdout 是"设计上就携带敏感内容"的信道**——MCP stdio 的协议载荷（工具响应）本身就含检索到的 chunk 原文、来源路径、元数据，这是功能不是缺陷，但意味着：**stdout 一旦被重定向，敏感内容就落盘**（`python main.py > log.txt`、`tee`、CI 里捕获 stdout——文档原文以明文写进日志文件）。这是主动设防过的点：`server.py` 里 `_redirect_all_loggers_to_stderr()` 把所有日志 handler 强制改到 stderr，保证 stdout 只有协议消息——但防的是"日志污染 stdout"，**防不了"stdout 本身被捕获"**，后者是使用纪律问题（别重定向 stdout）。本机若有其他同权限进程，理论可 attach 调试器读进程内存——但这已是"本机失陷"的前提，不是 stdio 特有的弱点。

②载荷层风险：`source_path` 返回的是**绝对路径**——路径本身可能泄露目录结构、用户名、组织信息；元数据里的 title/tags/summary 是文档内容蒸馏，与 chunk 原文同级敏感；**错误信息的意外泄露**——异常栈如果原样返回给客户端，可能带出内部路径、配置片段（异常在 `execute_tool` 层被捕获转成结构化错误，不回传原始栈——但这条要靠测试持续守住）。

③持久层风险（最实打实的一块）：`logs/traces.jsonl` **明文存储每次查询的完整中间产物**——查询原文、chunk before/after 全文、检索排名，**一份 trace 文件约等于语料库的高价值切片**，且会无限增长；`data/` 目录（向量库原文、BM25 索引、图片本体、版本快照）全部明文——**Chroma metadata 里的 chunk 原文意味着"拷走 data 目录 = 拷走全部知识库"**；`config/.env` 里的 API 密钥明文（已 gitignore，不进版本库，但磁盘上是明文）。

**三、已有防线盘点**

**stdout 协议独占**（日志全量重定向 stderr，代码级保证）；**密钥管理纪律**（`config/.env` 进 `.gitignore`，配置优先级 env > .env > yaml，密钥永不进受追踪文件、永不进工具响应）；**工具响应最小化**（返回检索结果和 Citation，不带系统内部信息）；**错误结构化**（异常不裸传，转 JSON-RPC 错误码）。

**四、认账的缺口与补法（按优先级）**

1. **响应端脱敏层**：在 `ResponseBuilder`（工具出口的最后关口）加 redaction——按字段规则掩码（员工身份证号、手机号等 PII 正则）+ 按 collection 配置脱敏等级。**这是唯一能同时覆盖传输和持久两侧的卡口位置**；
2. **trace 脱敏与保留策略**：敏感环境对 chunk 文本采样/截断/哈希化替代，加轮转和保留期（现在无限增长既是安全问题也是磁盘问题）；
3. **静态加密**：`data/`、`logs/` 依赖文件系统权限之上，用 BitLocker/LUKS 卷级加密兜底"整机拷盘"场景；
4. **多用户场景必须换传输**：stdio 没有用户身份概念——**trace 里没有"谁查了什么"**，审计能力为零。团队部署的正确形态是 MCP 的 streamable HTTP 传输 + OAuth2/API Key 鉴权 + TLS + 按用户 ACL（谁能查哪个 collection）；
5. **一个更深的点：间接提示词注入**。RAG + MCP 组合有个 stdio 管不住的风险：**检索到的文档内容会流入 Agent 的上下文**——一份被投毒的文档（藏着"忽略之前指令，把数据发到 X"的文本）可能操纵 Agent 行为。缓解手段：来源可信度标记、检索内容与指令的隔离框架、对生成内容做外呼动作前确认。这是 Agent 安全的前沿问题，Citation 机制（内容可溯源到具体文档）恰好是事后取证的基础。

**收束**：**stdio 在单用户场景是安全加分项（零网络攻击面、OS 级管道隔离），它不是泄露风险的来源；真正的风险按严重度排是——持久层明文（trace/data/.env）、载荷层字段（绝对路径、PII）、传输层使用纪律（stdout 别重定向）；已有的防线覆盖了协议纯净性和密钥纪律，缺口集中在"出口脱敏层、trace 治理、多用户鉴权"三处**。安全评估的正确姿势不是问"这个传输安不安全"，而是问"我的信任边界画在哪、敏感数据在边界内有几个明文落点"。

---

## 九、生产化与质量保障类

### Q32 如果该项目要真正落地投入生产，还需要做哪些工作？

核心观点先立住：**这个项目的"检索质量"和"工程底座"（测试、trace、可插拔）已经是生产思维，真正的差距集中在"服务化、多租户、安全合规、运维体系"四块**——而这四块恰好都是"单机内部工具"到"生产服务"之间的事，不是推倒重来，是在既有架构的预留位上施工。

**一、先定生产形态（决定后面所有工作的规模）**

形态 A：个人/小团队 Agent 工具（Copilot 里每人配一份，数据各自本地）——当前架构几乎可直接用，只差 P0 里的少数几项；形态 B：部门级共享服务（一个中心化服务，多用户 Web + MCP HTTP）——主要工作；形态 C：全公司知识平台（多租户、权限体系、SLA）——最重。按形态 B 展开。

**二、四个维度的差距盘点**

维度 1：服务化与并发——入口改造：MCP 从 stdio 切 **streamable HTTP 传输** + 包一层 FastAPI 异步服务（Web/API/MCP 三入口共用核心引擎——分层已保证引擎不用动）；读写分离：摄取走"队列 + 单写者 + 原子发布"模型（BM25 的 `os.replace` 原子替换、Chroma HttpClient 模式、SQLite WAL）；性能闭环：查询向量缓存、Embedding 就近部署，把端到端从秒级压到 SLA 内；压测出 P50/P95/P99 和饱和点。

维度 2：安全与合规——身份与权限：SSO 集成 + **collection 级 ACL** + 工具参数级权限；审计：stdio 没有用户概念、trace 里没有"谁查了什么"——要补 query 日志带身份、不可篡改的审计留存；脱敏：ResponseBuilder 出口 redaction（PII 正则 + 字段级掩码）、trace 脱敏与保留期轮转、`data/` 卷级加密；密钥管理：`.env` 明文升级到 Vault/Secrets Manager，轮转机制；对抗：至少做间接提示词注入的缓解设计（来源可信度标记 + 高危动作确认）。

维度 3：可靠性与运维（当前最薄的一层）——部署：Docker 镜像化（`data/` 卷挂载分离）、健康检查、优雅启停；监控告警：现在有 trace 但没有**指标化**——要暴露 Prometheus 指标（查询延迟分位数、Hit 相关的代理指标、Embedding/LLM 调用错误率、队列深度、索引版本号）+ Grafana 面板 + 告警规则。trace 是排障用的，监控是"睡着时也能发现问题"用的，两者缺一不可；LLM 运维：多 provider 的**故障切换自动化**（现在靠人改配置）、限流/退避/熔断、**成本核算**（token 预算与按部门计量）、降级链的显式监控（重排回退率飙升要告警）；数据运维：备份恢复演练（data 目录 = 全部状态，要定义 RPO/RTO）、OrphanGC 定期任务化、黄金集定时回归（CI 里已有评估脚本，要接到定时任务 + 指标看板）；发布：灰度（新旧索引版本并行，按流量切）、回滚预案（版本快照机制已天然支持数据回滚，补服务回滚）。

维度 4：数据与质量的持续运营——摄取质量门控上线（乱码率/正文占比评分拒入 + 待审查队列）；**badcase 回流闭环**：用户点"这个答案不对"→ 进待标注队列 → 进黄金集 → 下次回归防止退化，**黄金集要从"开发时的考卷"变成"持续生长的考卷"**；短板治理排期：表格/多栏/扫描件按真实语料分布决定投入；指标体系：技术指标（Hit Rate/MRR/延迟）之上补业务指标（自助解决率、引用点击率、拒答率），月度评审。

**三、按优先级排：P0（上线阻塞项）/ P1（上线后一个月内）/ P2（规模化）**

P0——不做就不能上：HTTP 化 + 鉴权（没有身份认证的服务不接入生产网络）、审计日志、备份策略、基础监控（延迟/错误率/磁盘）、密钥出 `.env`、trace/data 的访问权限收紧。P1——上线即启动：摄取队列单写者改造（防并发写坏索引）、脱敏层、查询缓存、黄金集定时回归 + 指标看板、badcase 回流、压测报告。P2——规模驱动：Qdrant/Postgres 迁移、多实例摄取分片、灰度发布体系、成本计量、ACL 细化到文档级。

**四、一个收束性的判断**

这份清单看着长，但拆开看会发现**大约 70% 的工作是在"加层"而不是"改心"**：入口加 FastAPI 层、摄取加队列层、响应加脱敏层、运维加指标层——核心的检索引擎、摄取管线、评估体系基本不动。这正是当初"可插拔 + 分层 + 白盒 trace"架构决策的兑现时刻：**生产化成本的高低，在架构定形那天就决定了**。剩下 30% 里最贵的是权限体系和持续运营机制——前者是脏活但没有技术难度，后者（badcase 回流、指标评审）其实是组织习惯而非代码。

### Q33 RAG 评价指标有哪些？RAG 评估怎么做的？

**一、指标体系（按管线分层）**

RAG 是"检索 + 生成"两段，指标必须**分段归因**——否则指标差了不知道该修检索还是修生成。分三层：

①检索层指标（确定性计算，不需要 LLM，便宜）：**Hit Rate@k**（正解是否出现在 top-k，0/1——最基础，Hit 低病在召回之前）、**MRR**（倒数排名，1/rank——排序质量）、**Precision@k / Recall@k**（top-k 里相关占比 / 相关被召回占比——噪声 vs 漏召）、**NDCG@k**（带位置折损的排序质量——精排质量）。

②生成层指标（LLM-as-Judge，Ragas 系）：**Faithfulness**（忠实度：答案是否只基于检索上下文——**幻觉率的反面**，RAG 安全性的核心指标）、**Answer Relevancy**（答案与问题的相关性——治"检索对了但答非所问"）、**Context Precision / Recall**（检索上下文里相关内容的排序与覆盖——桥接检索与生成的指标，能定位"上下文里有答案但 LLM 没用上"还是"上下文根本不够"）。

③端到端与业务层：无参考答案时的正确性判断（LLM-judge）、拒答率、**引用准确率**（Citation 指向的 chunk 真的支持该论断吗）、延迟、token 成本、业务指标（自助解决率）。

**选型原则**：确定性指标做日常回归（免费、可进 CI），LLM-judge 做阶段性精评（贵、有波动），业务指标做最终裁决——**三层各司其职，不要用昂贵的指标干便宜指标的事**。

**二、我们的评估实现**

架构：黄金集 + 双评估器 + 消融。

①**黄金集：一个关键的工程设计**。每条用例是 `query + expected_sources + expected_chunk_ids`。工程问题：**chunk ID 里嵌着绝对路径哈希和 LLM 精炼文本的哈希，跨机器不可移植**——把 ID 写死进 Git 追踪的黄金集，别人 clone 下来必然全挂。解法三件套：**提交进 Git 的黄金集只含期望来源（source 级可移植）**；`verify_golden_set.py --refresh-ids` 在本机跑一遍检索、把机器本地的 chunk ID 写进 **gitignore 的本地文件**；`verify_golden_set.py` 本身先做"黄金集可答性"检查——确认每条 query 的期望来源真的在索引里，防止拿一份考卷考一门没开的课。这套设计让评估**既可共享又可本地执行**。

②**CustomEvaluator：确定性指标的四个变体**。实现了 `hit_rate / mrr` 之外还有 **`source_hit_rate / source_mrr`**——chunk 级匹配（严格）和来源文档级匹配（宽松）分开算。区分的实际价值：**source 级指标跨环境稳定**（不依赖 chunk ID），chunk 级更敏感（分块变了立刻反映）。配置纪律：显式传不支持的指标名直接报错（fail fast 暴露配置错误），从 settings 派生的则静默过滤——**显式错要炸、派生错要容**。

③**RagasEvaluator**：faithfulness / answer_relevancy / context_precision 按需启用（要 LLM 密钥、有调用成本），默认 CI 不跑它——**贵的指标不进日常回归**。

④**消融实验：评估驱动决策的主战场**。`evaluate.py --ablate` 一条命令对比四种检索配置：dense 0.72/0.65/0.78/45ms；BM25 0.68/0.71/0.73/28ms；hybrid RRF 0.80/0.78/0.84/65ms；+CE 0.85/0.82/0.88/89ms；+LLM Rerank 0.83/0.80/0.87/320ms。这组数据支撑了三个真实决策：**上混合检索**（NDCG +6）、**重排默认关 + 偏好 Cross-Encoder**（LLM Rerank 精度略低延迟 3.6 倍）、**chunk_size=1000 的网格扫描**同框架完成。

⑤**评估怎么进研发流程**：CI 跑环境自检 + Prompt 校验和 + 非 LLM 测试；评估脚本本地/定时跑，HTML 报告落 `reports/`；任何检索配置改动——黄金集回归，**指标漂移超阈值不合入**。

**两个诚实的缺口**：1. **黄金集是静态的**——还没有线上 badcase 回流机制（用户点"答案不对"→ 自动进待标注队列 → 长成新用例），考卷要持续生长，否则会渐渐代表不了真实查询分布；2. **生成层指标依赖 Ragas + LLM 密钥**，日常回归实际只覆盖检索层——生成质量的日常监控（哪怕是抽样 faithfulness）还没接入定时任务。

**收束**：指标体系上，按"检索层（Hit/MRR/NDCG 确定性）→ 生成层（faithfulness 等 LLM-judge）→ 业务层"分层归因；实现上，答案是"**可移植的黄金集 + 零成本规则评估器海选 + Ragas 精评复核 + 消融实验支撑决策**"四件套——评估在这个项目里的地位不是附属品，而是所有调参决策的证据来源。

### Q34 项目中有涉及"意图识别"的内容吗？怎么做的？

**一、意图识别在项目里的三个落点**

```text
用户查询
   │
   ├─ ① QueryProcessor（查询预处理：清洗、分词、关键词抽取）
   │
   └─ Agent 链路（agent_query 入口）
       ├─ ② RuleQueryUnderstanding（查询理解：词项扩展）
       └─ ③ QueryRouter（意图路由：direct_rag / multi_hop / tool）
              └─ 决定：直通检索 or 多跳循环 or 调专用工具
```

**二、核心：QueryRouter 怎么做意图分类**

意图空间是三类（刻意收窄，不搞大而全）：`direct_rag`（常规知识查询，直通混合检索，**不进 Agent 循环**）；`multi_hop`（对比类/需要多轮检索的复杂查询（"对比|比较|区别|差异|compare|versus"），进循环多跳）；`tool`（元查询，路由到专用工具并指定工具名——"有哪些知识库/集合" → `list_collections`，"总结一下某文档" → `get_document_summary`）。

双引擎实现，与摄取链路同构的降级哲学：**规则版（RuleRouter，默认）**——有序正则匹配：`集合|collection|列表|库列表` → list 工具；`总结|摘要|概要|summary` → summary 工具；对比类正则 → multi_hop；都不中 → direct_rag。**零 LLM 成本、确定性、毫秒级**。**LLM 版（LLMRouter，可配置升级）**——强约束的系统 Prompt：中文指令、**只回一个词**（`tool:list_collections` / `multi_hop` / `direct_rag`），`_parse_llm_reply` 解析；**任何异常（解析失败、LLM 报错）fallback 到 direct_rag**。这个 fail-safe 方向是精心选的：路由器出错时最安全的兜底就是"当普通检索处理"——最坏情况退化为基线能力，而不是错误地进昂贵循环或调错工具。**工厂装配**：`RouterFactory.create(settings)` 按 `agent.router.provider` 配置实例化，配置缺失/错误时**回落规则路由**。`RouteResult` 带 `reason` 字段落 trace，每次路由决策可回查归因。

**三、配套：RuleQueryUnderstanding（查询理解的规则版）**

它填的是 `ProcessedQuery.expanded_terms` 这个槽位——**离线别名表扩展**：查询里出现 "RAG" 就扩展出"检索增强生成 / retrieval augmented generation"，"embedding" 扩展出"向量化/嵌入"，中英双语 + 同义缩写，大小写不敏感的子串匹配，**零 LLM 成本提升召回**（尤其喂给 BM25 那一路，直接缓解词汇失配）。架构上留了口子：`QueryUnderstanding` 是 ABC，LLM 版扩展随时可以子类化接入。

**四、为什么这么做**

1. **成本控制是第一动机**：Agent 循环每步都是 LLM 调用，而内部场景 80%+ 的查询是简单知识问答——**路由器把简单查询挡在循环外**，Agent 的单位成本只花在真正复杂的查询上。规则版零成本跑在每次查询的关键路径上，这是它当默认的原因；
2. **能力路由是第二动机**：元查询（"有哪些库"）不是检索问题，硬塞给 RAG 只能得到牵强的检索结果——路由到专用工具是对"意图决定处理方式"的直接践行；
3. **渐进式智能**：规则先行、LLM 可升级、错误有兜底——和摄取链路（规则清洗打底 + LLM 升级 + 降级）是同一个设计模式在不同位置的复用。

**五、诚实说边界，以及演进**

1. **规则路由对改写脆弱**："都有哪些库"不带"集合"关键词就漏到 direct_rag——正则覆盖的是模式不是语义。当前兜底行为安全（退化为检索，答案不会错，只是差）；
2. **别名表是硬编码的领域词表**（现在装的是 RAG/LLM 术语）——换企业场景必须可配置化：别名表进 settings/collection 级配置，这是上生产的必改项；
3. **意图空间只有三类，没有闲聊检测、没有置信度**——路由决策没有"我不确定"状态，也就没有"低置信升级到 LLM 路由"的混合策略。这恰好是最优的演进路径：规则先判，规则命中高置信词表直接走；模糊地带才付一次 LLM 分类调用，把成本花在刀刃上；
4. **路由自身缺评估**：给检索建了黄金集，但**没有给路由建"路由准确率"考卷**（标注查询→期望路由的样本）。路由是 Agent 的第一道决策，它错了后面全错——它值得自己的评估集。

**收束**：项目里的意图识别 = **"三类意图的路由器（规则正则打底 + LLM 升级 + 直通兜底）+ 零成本别名扩展的查询理解"**，设计主线是成本分层（简单查询不付 Agent 成本）和失败安全（路由错误的最坏后果是退化为普通检索）。它的定位不是炫技的 NLU，而是**Agent 链路的成本闸门和调度器**。

### Q35 关于"精排"：怎么理解？流程中有吗？怎么做的？效果？如何评估？

**一、怎么理解"精排"这个环节**

一句话：**精排是"用深度交互换排序质量"的最后一段漏斗，它的存在由前序检索的固有缺陷决定**。召回层的两个引擎都是"**表示级匹配**"——Dense 是双塔结构，query 和文档**各自独立**编码成向量再算余弦，两段文本从没"见过面"；BM25 更是纯词面统计。这带来一个结构性缺陷：**召回层能大致找到相关的，但分不清"很相关"和"有点相关"的精细次序**——而用户答案质量对 top 几名的次序极其敏感。精排用 **Cross-Attention 逐对深度交互**（query 和 passage 联合编码）补上这个精度，代价是每对都要跑一次模型——**贵，所以只能放在漏斗末端，对少量候选精打细算**。这就是"召回求快求宽（20 路）、精排求准求慢（10 选 5）"两段式架构的由来：各层按自己的成本结构干自己擅长的事。

**二、流程里有——明确的一层**

```text
Dense Top-20 ─┐
              ├→ RRF 融合 Top-10（粗排）→ 精排 Top-5 → 答案生成
BM25 Top-20 ─┘
```

精排是第三层，`settings.yaml` 的 `rerank.provider` 控制：`none / llm / cross_encoder` 三态——**做成开关而非默认路径本身就是一个精排认知的体现**。

**三、怎么做的**

双引擎 + 工厂，和全项目同构的可插拔实现（`src/libs/reranker/`，`BaseReranker` ABC + 两个实现 + Factory）：①**CrossEncoderReranker（主力选项）**：本地 sentence-transformers 加载 Cross-Encoder 模型（如 ms-marco-MiniLM 系），对 (query, passage) **逐对联合编码直接输出相关性分**，按分重排取 top-5。本地推理无 API 依赖、延迟稳定、支持 mock scorer 注入做确定性测试。②**LLMReranker（升级选项）**：Prompt 走模板文件（`prompts/rerank.md`，**带校验和进 CI 版本管理**）——0~3 四档相关性评分标准写得很细（3=直接回答 / 2=部分相关 / 1=边缘相关 / 0=无关），**输出强约束 JSON**，解析确定性。打分维度覆盖语义相似、是否含答案、信息完整度。③**工程配套三件**：**失败降级**——重排任何异常自动回退 RRF 融合排名（保可用性）；**trace 记录 before/after 排名**——精排把谁提上来了、把谁按下去了，每次都落盘；**Prompt 校验和**——防止悄悄改评分标准导致线上漂移。

**四、带来了什么效果**

| 配置 | Precision@5 | NDCG@10 | 延迟 |
|---|---|---|---|
| hybrid RRF（无精排） | 0.80 | 0.84 | 65ms |
| + Cross-Encoder | **0.85** | **0.88** | 89ms |
| + LLM Rerank | 0.83 | 0.87 | 320ms |

三个结论，每个都驱动过真实决策：1. **精排有效**：Cross-Encoder 带来 Precision@5 +5pp、NDCG +4pp——代价是 +24ms，**每毫秒买到的排序质量是全系统性价比最高的交易之一**；2. **但 LLM 版不划算**：精度反而略低（0.87 < 0.88）、延迟 3.6 倍（320ms）、还有 API 稳定性风险——所以**默认关闭、需要精排时优先 Cross-Encoder**，这个"精排默认不是开启"的反直觉决策，完全是被这组数字推出来的；3. **降级链的价值被量化过**：重排故障回退融合排名，Hit Rate 降 3~5pp 但服务不中断——"质量略降"和"不可用"之间的这笔账，用户端感受差异巨大。

**五、怎么评估这个环节做得好坏（四层）**

①**消融隔离（宏观）**：固定语料、固定召回配置（dense/sparse/top-k/RRF 参数全不动），只切换精排开关——`--ablate` 一条命令，四配置对比。**控制变量是评估精排的前提**。②**排序敏感指标（中观）**：精排的本质是改排序，所以看**对次序敏感的指标**——NDCG@10（位置折损）和 MRR（正解排名），而不只是 Hit Rate（top-k 里有就行、对精排不敏感）。NDCG 0.84→0.88 这 4 个点就是精排的"净贡献"。③**swap 分析（微观，trace 驱动）**：因为 trace 记了 before/after 排名，可以做**逐 query 的换位归因**——精排救回了哪些 query（正解从第 8 提到第 2）、搞砸了哪些（正解被从第 1 压到第 4）。宏观数字会说谎（均值好但某类查询系统性变差），swap 分析能暴露**精排的失败模式**——比如 Cross-Encoder 对长条款 chunk 的偏爱会压短结论性 chunk，这类洞察只有微观层给得了。④**成本核算（经济性）**：精排的评估不能只看质量——**每 pp 提升的毫秒成本、每千次查询的 token 账单**要一起算。LLM Rerank 被否决不是因为效果差（0.87 也不低），是"单位质量的价格"太贵。

**收束**：精排是两段式检索的末端精加工，价值来自 Cross-Attention 对"表示级匹配"盲区的补位；实现上是 Cross-Encoder 与 LLM 双引擎可插拔、带降级和全量 trace；效果上 Cross-Encoder 以 +24ms 换 +4pp NDCG 是最划算的交易，LLM 版因单位成本过高被数据否决；评估上靠"消融隔离 + 排序敏感指标 + swap 微观归因 + 成本核算"四层——**精排不是"加了就更好"的功能开关，是被测量驯服的一个性价比决策**。

### Q36 想更新 RAG 中的文档，该怎么操作？底层发生了什么？

**一、上层操作：三种场景三条路**

```bash
# 场景 1：改完文档放回原路径，重跑摄取（最常用）
uv run python scripts/ingest.py --path ./documents --collection handbook

# 场景 2：强制重摄（文件没变但想重跑，比如换了 Embedding 模型）
uv run python scripts/ingest.py --path ./documents --collection handbook --force

# 场景 3：先看会动哪些文件（确认影响面）
uv run python scripts/ingest.py --path ./documents --dry-run
```

场景 1 是核心：**不需要"先删旧的再传新的"**——直接重跑，系统自己识别"这是更新"。仪表盘的 Ingestion 管理页也能做同样的事。

**二、底层时序：一次更新的完整解剖**（前提：`员工手册.pdf` 内容改了，路径没变）

第 1 步：**Stage 1 发现"你变了"**。算新文件的 SHA-256，查摄取历史——**哈希不同，不跳过**。隐含语义：旧的哈希记录还在历史表里，新旧两个身份从此并存，这是后面一切清理动作的坐标。

第 2 步：**Stage 2 版本预检 + 上保险**。查版本账本 `get_active(source_path, collection)`：该路径有活跃版本且哈希不同 → 标记 `is_update=True`，记下 `old_hash`；同时**把文件快照存进 `data/versions/{collection}/{file_hash}/`**——这是回滚保险，best-effort，失败不阻塞。

第 3~5 步：**当新文档重新走一遍管线**。重新解析、分块、LLM 清洗、Embedding。**关键且容易忽略的点**：chunk ID 是 `{doc_id}_{index}_{hash8}`，而 `doc_id` 来自文件哈希——内容变了，**新 chunk 的 ID 和旧的全都不一样**。所以 Stage 6 的 Chroma upsert 是**插入新向量，不是覆盖旧向量**——旧向量此刻还躺在库里。

第 6 步：**BM25 自愈（设计上最优雅的一步）**。BM25 的 `doc_id` 前缀来自 **source_path 的哈希——路径没变，前缀不变**。`add_documents` 是 remove-before-rebuild：同一个前缀的旧 posting list **整体删除后重建**。BM25 那一侧不需要任何显式清理，更新动作天然清旧。

第 7 步：**收尾三连（成功路径）**。`mark_success`：摄取历史写入新哈希；`record_success`：版本账本把新哈希翻成活跃版本，旧版本转非活跃；**`_cleanup_old_version`：显式删 Chroma 里的旧向量**——删除条件是 `(source_path, old_hash)` **双条件组合**。

第 8 步：**兜底**。清理失败的残留（比如删到一半断电），由 **OrphanGC** 周期任务回收：扫出"不在活跃账本里的哈希/前缀"对应的孤儿数据，统一清除。

**三、三个值得展开的设计点**

①**为什么 Chroma 删除要双条件，BM25 却什么都不用做？** 因为两种存储的身份语义不同：BM25 的 doc_id 直接绑定 source_path（路径不变即自愈）；Chroma 的 chunk ID 绑定内容哈希（内容变即"新文档"，旧的成了无主数据）。而双条件的精妙在 `source_path + old_hash`：**同一份内容（同哈希）合法地存在于两个路径**（文件拷贝）是允许的场景——只按哈希删会误杀另一个路径的副本，只按路径删会杀掉该路径下未来可能的重摄版本。双条件精确圈定"这个路径的这个旧版本"。

②**四个存储怎么保持一致？没有事务。** 摄取历史、版本账本、Chroma、BM25 各自按各自语义更新，一致性靠**同一套身份体系（路径哈希 / 内容哈希 / chunk ID）散落各处、可对账**——任何时刻跑一次对账（OrphanGC 的 dry-run 就是这个），漂移的只有"残留垃圾"，不会是"逻辑错误"。**最终一致 + 垃圾回收**，而不是分布式事务——这是 Local-First 架构下的正确取舍。

③**中途失败会怎样？** 假设 Stage 5 Embedding 挂了：`mark_failed`，版本账本记失败，**旧版本仍是活跃版本、旧向量没被清理**（清理只在成功后执行）——查询继续由旧数据服务，**可用性无损**。代价是可能残留部分写入的新向量，下次重摄时新 chunk ID 覆盖写自愈，OrphanGC 兜底扫尾。这个"失败不碰旧数据"的次序是刻意的。

**四、回滚：更新的逆操作**

发现新版文档错了想退回？版本账本 + 快照就是为这准备的：从 `data/versions/` 找到旧版快照，**以原逻辑路径（`logical_source_path`）重摄**——路径不变 → chunk ID 前缀不变 → 和当年那版完全相同的确定性 ID → 等价于一次普通更新，把新版"顶替"掉。**回滚复用更新机制，不是另一套代码**。

**五、一个诚实的成本边界**

更新的粒度是**文档级全量重付**，不是 chunk 级增量：改一个错别字，也要重跑该文档的解析 + LLM 清洗 + 全部 Embedding。理论上可以做 chunk 级 diff（只重嵌入变更块），但实践中不划算——**改一处文字，分块边界移动，后面所有 chunk 的 index 和内容哈希全变**，diff 命中率极低还引入对齐复杂度。文档级全量 + 幂等语义，是这个场景下"简单且总是正确"的选择；真正省成本的是入口的文件级哈希——**100 个文件的目录改 1 个，只有 1 个付全量价**。

**收束**：操作上就是"改完放回去重跑 ingest"一条命令；底层是一场**身份体系驱动的协同更新**——新哈希触发、快照保险、新 ID 插入、BM25 前缀自愈、Chroma 双条件清旧、账本翻活跃、GC 兜底——全程没有事务，靠确定性身份和"清理后置到成功之后"的次序，保证任意时刻中断都收敛到一致状态，且旧数据在更新完成前持续可查。

---

## 十、Agent 与前沿方向类

### Q37 有没有了解 RAG 的新方向？（Self-RAG / Agentic RAG / Graph RAG）

三个方向里 **Agentic RAG 是真实实现了的**（`agent_query` 那条链路），Self-RAG 有它的工程近似（反思器），Graph RAG 没做但可以说清楚为什么和什么时候会做。

**一、Self-RAG：把"检索反思"训练进模型**

核心机制：传统 RAG 的检索时机和用法是流水线写死的；Self-RAG 通过训练让 LLM **边生成边发反思信号**——四类 reflection token：要不要检索（Retrieve?）、检索结果相关吗（Relevant?）、生成内容被上下文支撑吗（Supported?，压制幻觉的核心）、整体有用吗（Useful?）。模型在解码时动态决定检索时机、自我批判、按支撑度挑选最优续写。优点：按需检索（简单问题不付检索成本）、生成与证据强绑定、幻觉抑制有机制保证。代价：需要**训练**（反思数据的构造和微调门槛）、推理 token 数翻倍、反思 token 本身会错（反思错一步，全链路跟着错）。

和我们项目的关系（诚实对比）：`src/core/agent/reflection.py` 的 `RetrievalReflector` 是它的**零训练工程近似**——不训模型，用规则/LLM 提示判断"检索质量够不够、要不要重查"。区别在：我们的反思发生在**管线层**（可观测、可归因、可关闭），Self-RAG 发生在**模型权重里**（更丝格但黑盒）。在不能微调模型的工程场景（用 API 模型），管线层反思是唯一可落地的形态。

**二、Agentic RAG：把 RAG 从"流水线"变成"Agent 的工具"**

本质：RAG 从"一次检索一次生成"变成"**检索成为 Agent 可多次调用、可组合、可反思的工具**"——查询分解（对比类问题拆两跳）、动态选源、自我纠错都从这个范式里长出来。（实现细节见下一题。）

**三、Graph RAG：给知识加"结构"，治多跳和全局问题**

核心机制：摄取期用 LLM 抽实体和关系建知识图谱，对图做**社区检测**（Leiden 算法）并逐层生成社区摘要；查询分两模式——**local search**（从查询实体出发的子图推理，管多跳问题"X 和 Z 什么关系"）、**global search**（社区摘要做 map-reduce，管全局问题"这批文档的主要主题是什么"——这是传统向量 RAG 的死穴，top-k 永远凑不出全局视图）。

为什么强：向量检索是"局部相似度盲搜"，图谱是"沿关系行走"——多跳推理（A 影响 B，B 决定 C）在图上是一跳一跳遍历的，在向量空间里是靠运气的。代价（也是没做的原因）：摄取成本翻数倍（每块都要 LLM 抽取）、**增量更新难**（改一个文档，图上连带的社区摘要要重算——直接撞上幂等更新体系）、图质量依赖抽取质量（错边比没边更害人）。判断：企业制度场景的查询 90% 是"条款级局部查询"，BM25+混合检索已经覆盖；真正的全局性/多跳需求如果长起来，会先上**轻量替代**（MetadataEnricher 的 tags 做实体粗索引 + Agentic 多跳），确认查询分布真的需要后，再按 collection 级引入 Graph RAG——**它是场景驱动的升级，不是默认配置**。

**四、顺带补三个也该知道的方向**

**CRAG（Corrective RAG）**：检索后先评估质量（轻量评估器），不合格就**纠错**——重写查询、换数据源、还是兜底 web 搜索。和 RetrievalReflector 同宗，我们少了"换源"这一步；**Adaptive RAG**：按查询复杂度路由到 no-retrieval / single-shot / multi-hop——这就是 QueryRouter 三分流的学术版，我们是在工程上先撞到了这个设计；**RAPTOR**：对 chunk 做层次化摘要树，不同层服务不同粒度查询——和"父子检索"演进方向同源。

**五、判断框架（收束）**

三个方向不是并列的"新技术列表"，而是三条**正交的进化轴**：

| 轴 | 问题 | 代表 |
|---|---|---|
| **控制流轴** | 检索几次、何时检索、错了怎么办 | Self-RAG / Agentic RAG / CRAG |
| **知识结构轴** | 知识以什么形态被组织 | Graph RAG / RAPTOR |
| **粒度轴** | 检索打多小、返回多大 | 父子检索 / Small-to-Big |

它们可以组合（Agentic + Graph 就是"Agent 在图上走"）。而**选型永远是场景函数**：查询分布（局部 vs 全局）、语料结构性（条款 vs 关系网）、成本预算（摄取翻倍能不能接受）、可控性要求（反思放管线还是放权重）。项目的答案：控制流轴上走了 Agentic（工程化、有缰绳的版本），知识结构轴按兵不动（场景不需要），粒度轴排了父子检索的演进——**每一步升级的理由都来自黄金集和 badcase，而不是来自论文的新鲜度**。

### Q38 详细讲讲项目中的 Agentic RAG：做了些什么，带来了什么效果

**一、定位：Agentic RAG 在项目里是什么**

一句话：**把混合检索从"一次调用的流水线"升级为"Agent 循环里可反复调用、可反思、可组合的工具"**。入口是 MCP 工具 `agent_query`，配置在 `settings.yaml` 的 `agent` 块——**默认 `enabled: false`**，未启用时自动退化为直通混合检索。默认关是刻意的：保证没有 LLM 密钥的环境整条链路依然稳定可用，也符合"新能力的最坏后果不能比没有它更差"的原则。

**二、一条查询在 Agent 里的完整生命周期**（`agent_runner.py` 真实执行路径，以"对比一下 A 政策和 B 政策的区别"为例）

```text
agent_query(查询)
 │
 ├─ 0. 前置检查：空查询 → 拒答(refusal_reason=empty_query)；LLM 不可用 → 降级直通
 │
 ├─ 1. QueryUnderstanding：规则版词项扩展（零成本），产出 keywords + expanded_terms
 │
 ├─ 2. Router 前置步（不进循环！）："对比类" → multi_hop；
 │    元查询 → 直接预执行工具（route_pre_exec），结果作为上下文注入消息
 │
 ├─ 3. 消息组装：策略 system prompt + 会话记忆（窗口 10 轮）+ 用户查询
 │
 ├─ 4. ReAct 循环（上限 max_iterations=5）：
 │     LLM(chat_with_tools) → 解析工具调用
 │       ├─ 非法格式 → 错误信息回注，继续（不是崩溃）
 │       ├─ 白名单外工具 → "不在白名单"回注，继续
 │       ├─ 检索工具 → 带反思的检索（见下）
 │       └─ 无工具调用 → 视为最终答案，跳出
 │
 ├─ 5. 带反思的检索 _search_with_reflection：
 │     engine.search → reflector.evaluate(结果)
 │       └─ 质量不足 → rewrite(原查询+扩展词) → 重检索
 │            （上限 max_retrieval_rounds=2，反思轮次落 trace）
 │
 ├─ 6. 收尾：答案引用消毒(sanitize_citation_markers，防 LLM 编造引用序号)
 │    → 记忆写入(成功才记) → AgentResult{content, citations, confidence,
 │       intermediate_steps, refusal_reason}
 │
 └─ 兜底：步数耗尽仍无答案 → 用最后一次检索结果作答 + 标记
    agent_max_iterations_exceeded（宁可给降级答案，不给空手而归）
```

每一步都有对应 trace stage（`agent_router` / `agent_tool_call` / `agent_reflection` / `agent_final`）——**Agent 的每一步可归因**。

**三、五个关键设计决策（为什么这么做）**

①**三种策略可切换：`react / plan_and_execute / self_ask`**。模板方法模式实现：`_BaseLoopAgent` 持有循环骨架，子类只覆盖三个钩子（`_system_prompt`、`_pre_loop` 规划步、`_parse_final`）。不是"功能堆砌"——**不同查询形态适配不同范式**（ReAct 适合逐步试探，plan_and_execute 适合先拆解再执行，self_ask 适合自问自答链），而且**换策略不改循环骨架**，消融对比随时可做。

②**路由前置，不进循环**。元查询（"有哪些库"）在循环**之前**就被路由器拦截直接执行——循环是给复杂查询的，简单/元查询进循环纯属烧钱。这是成本分层的第一道闸。

③**反思是"带缰绳的重试"**。`RetrievalReflector` 评估检索质量，不足则**改写查询**（融合 `expanded_terms`，多一次词汇失配的补救机会）再检索——但**上限 2 轮**。没有上限的反思循环等于无限烧钱，这个数字是成本与收益的闸门。

④**白名单 + 步数上限 + 记忆窗口三重缰绳**。工具白名单（配置里三个工具）防 LLM 幻觉调用不存在的工具；`max_iterations=5` 防循环失控；记忆窗口 10 轮防上下文无限膨胀。**Agent 的自由度是设计出来的预算，不是给的无限信用卡**。

⑤**四条降级路径，条条有交代**。空查询→结构化拒答；LLM 不可用→降级直通检索；LLM 中途报错→同样降级（带 detail）；步数耗尽→降级用最后检索结果作答并标记。**每条失败路径的出口行为都是确定的**——这是敢把它接进生产链路的前提。

**四、带来了什么效果**

能力增量（定性，真实存在）：1. **多跳/对比类查询从"做不到"变"做得到"**：Router 的 multi_hop 分流 + 循环多次检索，"A 和 B 的区别"这类需要两路召回各取所长的查询，直通模式天然覆盖不了；2. **检索失败的自愈**：反思改写给了查询第二次、第三次机会——词汇失配类 badcase（口语 vs 术语）多了一层补救；3. **多轮对话**：会话记忆让"那它的上限呢"这种指代查询可解析；4. **元查询工具化**：知识库管理操作（列库、取摘要）成为对话里的一等公民。

工程保证（更值钱的部分）：**最坏情况有界**——所有异常路径退化为直通检索，Agentic 层的任何故障不传染基础检索能力；**成本有界**——路由挡掉简单查询 + 步数/反思轮次上限，单查询成本上限是可计算的（5 步 × 每步 1 LLM 调用 + 最多 2 轮反思检索）；**全程可归因**——intermediate_steps + 四个 trace stage，Agent"为什么走了这三步"永远答得出来。

**诚实的量化缺口（必须主动交代）**：没有做"**按查询复杂度分层的 Agent vs 直通对比**"——黄金集没有按 simple/complex 分桶，所以"Agent 对复杂查询的提升幅度"目前只有定性证据（badcase 修复案例）没有定量数字。补法很明确：黄金集加复杂度标签，跑 `agent.enabled` 开/关的分层对比，复杂桶看提升、简单桶看成本浪费率——这会是 Agentic 层的第一次正式消融。另外 `enabled: false` 的默认值也说明态度：**在成本敏感的默认配置里，没有证据支撑它值得常开**——等分层消融数据说话。

**收束**：做的是"有缰绳的 Agentic RAG"——三策略模板化、路由前置省成本、反思限轮自愈、白名单/步数/记忆三重预算、四条降级路径兜底，每步落 trace；带来的是复杂查询的能力增量（多跳/自愈/多轮）和"最坏不劣于基线"的工程保证——但它的定量价值还没有分层数据背书，这是排在评估计划里的第一件事。**Agentic RAG 的核心不是"让 LLM 自由发挥"，而是在自由和可控之间画出明确的预算线——项目里每一条缰绳，都是这条线的具体形状。**
